---
work: tolkien
status: CONFIRMED
tiers_active: [1, 2, 3, 5]
---

# Greenlight — The Lord of the Rings

> This is a Phase-3 SMOKE PROVE of the `/laf:prep` pipeline. The prove treats the greenlight
> confirmation as CONFIRMED (the prove plays both the coordinator AND the confirming user).

## Checklist

- [x] Source access declared — FULL (`laf-adaptation/source/tolkien/ch-01.txt`, 259 words, Phase-0 PASSED); text-track grounded.
- [x] Mapping confidence ceiling acknowledged — lowest effective confidence = PROBABLE (context-track ceiling per R6).
- [x] Tier set confirmed — `{1, 2, 3, 5}` (the default 4-tier set; no narrowing — the prove made no narrowing decision; T4 interpolated at request time, never stored).
- [x] Key decisions ratified — see `40-prep-brief.md § Decisions ratified` (allegory-stance, death-honor, battle-scope, target-age-nuance, omission-honor-set).
- [x] Compound-scene reconciliation baked into `10-challenges.yaml` for both compound scenes (Siege; Théoden's fall + Éowyn's stand).
- [x] Work-level `meaning:` present in `30-mapping.yaml` (6-KEY form); dual-form promotion targets will keep/strip `meaning:` per layer.
- [x] No canon or per-tier adaptation state written by the prep phase (boundary honored).

## User confirmation

> Phase-3 SMOKE PROVE — auto-confirmed. `status: PENDING → CONFIRMED`.

The dual-form promotion (`30-mapping.yaml` → the 0.1 hyphen 6-key kb copy + the root underscore
5-key copy) is executed as STAGE 7 by the coordinator itself (the form-transform operator), per
`path-contract.md` §3 + §5 and `prep-cordinator.md` Stage 7 (operator clarity).
