---
id: "TASK-RF-laf-hybrid-build-20260703-115948"
title: "Build LAF 0.1 (CWS Hybrid Integration, Path C) — Phases 0-4"
description: "Implement the LAF 0.1 architecture end-to-end across Phases 0-4, producing the laf-adaptation/ single-tree distribution: a vendored ADOPTED subset (patch-clean), a NATIVE adaptation spine (2 agents + 3 skills), BUILD-NEW greenfield (2 agents + 1 skill + genre resources), native kb layers (tiers, adaptation-mapping, per-tier canon graft G1), the VENDOR.md boundary contract + scripts/check_boundary.py enforcement gate, and the Phase-3 live-chapter hard gate (full 11-step workflow on one Tolkien chapter at T1, then T1/3/5 via tier-coordinator)."
version: "0.1"
status: "🟢 Done"
type: "🧩 Integration"
priority: "🔼 High"
created_date: "2026-07-03"
updated_date: "2026-07-04"  # All build phases 0-4 + 4 QA gates + Phase-3 hard gate GREEN; complete tree 15 agents/16 skills, boundary clean. In post-completion validation.
assigned_to: "orchestrator"
autogen: false
autogen_method: ""
coordinator: orchestrator
parent_doc: ""
parent_task: ""
depends_on: []
spec_path: ".dev/releases/current/0.1/design/DESIGN.md"
reflect_pre:
  verdict: "pass-with-remediation-applied — spec-complete (coverage_pct_union 0.97 ≥ 0.90 floor) and structurally faithful to DESIGN.md §7 Phases 0-4. PRE reflect returned PARTIAL with 3 blocking findings (A2 --init wording DRIFT, Q2 verifier cwd DRIFT, Q1 PC.6 auto-mutation NECESSARY-WITH-RISK) and 0 upheld REGRESSIONS (3 candidates downgraded after adversarial debate). All 3 blocking findings were remediated IN-PLACE by the orchestrator (A2: Step 2.2 --init wording corrected to 'hashes Step 2.3's grafted file, does not apply the graft'; Q2: Step 2.2 requires Path(__file__).resolve().parents[1] rooting for relative globs; Q1: Step PC.6 now runs audit-only --fix WITHOUT --promote for 0.1); the 2 unmapped_requirements_union were carried additively into ### Open Questions. Re-running the PRE gate post-remediation is expected to flip status to success. NOTE: the PRE gate is max-0-auto-loops per the task-builder protocol; these orchestrator-applied fixes are the authorized resolution path for evidence-cited DRIFT findings in the same class as the rf-* gate fixes."
  coverage_pct: 0.97
  depth: "deep"
  tcs: 785
  run_id: "pre-20260703192947-793c0654"
  report: ".dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/reflect/pre/adversarial/merged-verdict.md"
  reviewed_at: "2026-07-03T19:48:31Z"
  tier_reached: 2
  regression_present: false
  blocking_findings: 3
  blocking_findings_remediated: 3
  remediation_task: ".dev/tasks/to-do/TASK-RF-reflect-pre-remediation-laf-hybrid-20260703T194831Z/TASK-RF-reflect-pre-remediation-laf-hybrid-20260703T194831Z.md"
reflect_post:
  verdict: degraded
  status: success
  run_id: bf8d8b1a9ced
  tier_reached: 1
  report: /config/workspace/Infantalizer/.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/reflect/post/bf8d8b1a9ced/t2-swarm/reflect-review-01-qwen3.6-plus.final.md
  contract: /config/workspace/Infantalizer/.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/reflect/post/bf8d8b1a9ced/return-contract.yaml
  reason: degraded-tier1
  deviations:
    authorized: 0
    necessary: 0
    drift: 0
    regression: 0
  head: bf8d8b1a9ced25753f14e2bf9426bb235f666c2c
  reviewed_at: '2026-07-04T01:31:59.485924+00:00'
related_docs:
- path: ".dev/releases/current/0.1/design/DESIGN.md"
  description: "System overview, component inventory, distribution layout, data-flow, build phases & gates §7 (the spine of this task), residual risks §6, constraint-enforcement map §5"
- path: ".dev/releases/current/0.1/design/agent-schemas.md"
  description: "Frontmatter + bodies for analyst/safety-verifier (NATIVE §2), chronicler/tier-coordinator (BUILD-NEW §5), writer frontmatter-only patch (§3 ADOPTED-PATCHED), reader-sim persona-as-data (§4), dispatch-param summary (§6)"
- path: ".dev/releases/current/0.1/design/skill-specs.md"
  description: "SKILL.md bodies for /adaptation-tiers (§1), /adaptation-rules (§2 + thematic/character/agency resources), /source-fidelity (§3 v2.0 protocol + §3.2 output schema), /adaptation-safety (§4 + children.md/ya.md genre resources §4.1)"
- path: ".dev/releases/current/0.1/design/kb-formats.md"
  description: "kb/tiers/tier_N.yaml schema §2 (+ schema-drift §2.2, T4 interpolation §2.4), kb/adaptation-mapping cascade §3, per-tier canon graft G1 §4, shared-vs-per-tier split §5"
- path: ".dev/releases/current/0.1/design/boundary-contract.md"
  description: "VENDOR.md format §2, scripts/check_boundary.py verify algorithm rules A-F §3 (3 modes §3.1), enforcement points §4, upstream-sync protocol §5"
- path: ".dev/releases/current/0.1/design/safety-rubric.md"
  description: "6-section rubric §1, automatic failures §2, aggregation logic §3, machine-parseable verdict contract §4, workflow branch §4.1, traceability §5"
- path: ".dev/releases/current/0.1/design/tier-coordinator.md"
  description: "Reconciliation algorithm: inputs/outputs §1, fan-out modes §2 (parallel default / sequential G2 fallback §2.2), reconcile() 3 checks §3, output report §4, chronicler interaction §5, T4 handling §6"
related_prd: ""
related_tdd: ""
tags:
- "laf"
- "cws-hybrid"
- "path-c"
- "vendoring"
- "boundary-contract"
- "phase-0-4"
template_schema_doc: "/config/.local/share/pipx/venvs/superclaude/lib/python3.12/site-packages/superclaude/templates/workflow/02_mdtm_template_complex_task.md"
estimation: "large (multi-phase build + hard gate; ~47 named build-time files + upstream adopted resources)"
sprint: ""
due_date: ""
start_date: "2026-07-03"
completion_date: "2026-07-04"  # actual completion date (build finished 07-04 after the resume/POST-reflect; item's "2026-07-03" was the builder's same-day placeholder)
blocker_reason: ""
ai_model: ""
model_settings: ""
review_info:
  last_reviewed_by: ""
  last_review_date: ""
  next_review_date: ""
task_type: static
start_commit: "bf8d8b1a9ced25753f14e2bf9426bb235f666c2c (initial commit on main; reflect audit base — the reflect wrapper resolves its diff base from this field, so it MUST stay a git-resolvable 40-char SHA)"
executor_model_class: "opus (deep, multi-phase, hard-gate task)"
# reflect_post: room for the wrapper to write back the POST reflect verdict
---

# Build LAF 0.1 (CWS Hybrid Integration, Path C) — Phases 0-4

## Task Overview

This task implements the LAF 0.1 architecture (CWS Hybrid Integration, **Path C**) end-to-end across Phases 0→4, producing the `laf-adaptation/` single-tree distribution from a settled 7-spec design pack. The driving spec is `.dev/releases/current/0.1/design/DESIGN.md` whose frontmatter `next_step` is `/sc:implement @.dev/releases/current/0.1/design/DESIGN.md`. The design pack (DESIGN.md + 6 companion specs: agent-schemas, skill-specs, kb-formats, boundary-contract, safety-rubric, tier-coordinator) is the settled, [CODE-VERIFIED] source of truth for the Path-C decision.

The build mirrors DESIGN.md §7's phase table, which is the spine of this task: **Phase 0 (Vendor)** acquires the upstream CWS checkout, vendors 23 adopted files (11 agents + 12 skills) patch-clean with a deterministic prefix rewrite, authors the only script (`scripts/check_boundary.py`) and its `VENDOR.md` manifest, and runs `--init` green. **Phase 1 (Native Spine)** authors the 3 native skills (`/adaptation-tiers`, `/adaptation-rules`, `/source-fidelity`), the 2 native agents (`analyst`, `safety-verifier`), the `writer.md` frontmatter-only patch (ADOPTED-PATCHED), and ports the 5 native kb YAMLs + template verbatim from LAF's port-source `config/`/`templates/`. **Phase 2 (Greenfield)** authors the 2 build-new agents (`chronicler`, `tier-coordinator`), the `/adaptation-safety` skill + genre resources (`children.md`, `ya.md`), and scaffolds the native kb adaptation layers. **Phase 3 (Compose & Prove HARD GATE)** runs the full 11-step workflow on one Tolkien chapter at T1, then T1/3/5 via `tier-coordinator`, asserting all 5 gate conditions green — this is the definition of "0.1 done". **Phase 4 (Upstream-Sync Protocol)** documents the 6-step reconcile procedure.

This is an ADR-006 prompt + YAML-config framework — **NOT software**. The ONLY script is `scripts/check_boundary.py` (a validation tool, not a runtime). Native knowledge enters ADOPTED agents ONLY via `skills:` frontmatter, never body edits. The port-source YAMLs are carried VERBATIM (zero rework). The Phase-3 hard gate IS the integration test.

## Key Objectives

The following objectives MUST be achieved by this task:

1. **Vendor the 23 adopted files patch-clean (Phase 0):** Acquire the upstream CWS checkout at a pinned SHA, vendor 11 adopted agents (10 ADOPTED-CLEAN + `writer.md` ADOPTED-PATCHED) and 12 adopted skills (each with full `resources/**` subtrees) from CWS `cw/agents/`/`cw/skills/` (Claude-lowered, NOT Mars `agents/`), applying the uniform prefix rewrite `creative-writing-skills:` → `laf-adaptation:`. Author `scripts/check_boundary.py` and `VENDOR.md`, run `uv run python scripts/check_boundary.py --init --upstream <dir>` green.
2. **Author the NATIVE adaptation spine (Phase 1):** Create `analyst.md` + `safety-verifier.md` agents, `/adaptation-tiers` (+ 5 tier commentary resources) + `/adaptation-rules` (+ thematic/character/agency resources) + `/source-fidelity` skills, port the 5 native kb YAMLs (4 tier profiles + universal-mappings) + work-mapping template verbatim from LAF port-source.
3. **Author BUILD-NEW greenfield (Phase 2):** Create `chronicler.md` + `tier-coordinator.md` agents, `/adaptation-safety` skill + `children.md`/`ya.md` genre resources, scaffold native kb adaptation layers.
4. **Hold the Phase-3 HARD GATE (the definition of "0.1 done"):** All 5 conditions green — v2.0 CERTAIN/PROBABLE/UNCERTAIN tags present · safety PASS · per-tier canon written · all four quartet agents ran · `check_boundary.py` green — on a full 11-step T1 run plus a T1/3/5 tier-coordinator fan-out.
5. **Keep the boundary contract green throughout:** `check_boundary.py` exits 0 from Phase 0 through Phase 3; quartet intact (G3); every `agents/*.md` and `skills/**/SKILL.md` in the VENDOR.md manifest (Rule F); `writer.md` diff frontmatter-only and additive (Rule C).

## Prerequisites & Dependencies

### Parent Task & Dependencies
- **Parent Task:** none (this is a top-level release-build task driven by the 0.1 design pack)
- **Blocking Dependencies:** none — the design pack (`.dev/releases/current/0.1/design/`) is settled and present; the LAF port-source (`config/`, `prompts/`, `templates/`) is present in the repo.
- **This task blocks:** the 0.1 release; any downstream task that consumes the `laf-adaptation/` tree.

### Previous Stage Outputs (MANDATORY INPUTS)

**INFORMATIONAL ONLY - NO CHECKLIST ITEMS HERE**

**Required Previous Stage Outputs:**
- **Design pack (7 specs):** `.dev/releases/current/0.1/design/{DESIGN,agent-schemas,skill-specs,kb-formats,boundary-contract,safety-rubric,tier-coordinator}.md` — the settled, buildable source of truth for every artifact this task creates.
- **LAF port-source (carried VERBATIM, zero rework):** `config/age_profiles/tier_{1,2,3,5}_*.yaml` → `kb/tiers/tier_{1,2,3,5}.yaml`; `config/concept_mapping/universal_mappings.yaml` → `kb/adaptation-mapping/universal-mappings.yaml`; `config/concept_mapping/templates/tolkien_mapping.yaml` → onboarding `<work>-mapping.yaml`; `config/transformation_rules/{thematic,character}.yaml` → fenced resources; `templates/work_mapping_template.yaml` → `templates/work-mapping-template.yaml`; `prompts/{analysis/chapter_analysis,verification/safety_check,transformation/tier_{1,3}_transform}.md` → skill/agent bodies + genre resources.
- **Upstream CWS checkout:** acquired in Phase 0 (clone `https://github.com/haowjy/creative-writing-skills`, pin a SHA); provides the 23 adopted files vendored from `cw/agents/` + `cw/skills/`.

## Execution Context

### References
- [DESIGN.md](.dev/releases/current/0.1/design/DESIGN.md): system overview, distribution layout §2, 11-step data-flow §3, build phases & gates §7 (the spine of this task), constraint-enforcement §5, residual risks §6
- [agent-schemas.md](.dev/releases/current/0.1/design/agent-schemas.md): frontmatter + body specs for all 4 native/build-new agents, the writer ADOPTED-PATCHED graft (§3), reader-sim persona-as-data (§4), Claude-lowered dialect contract (§1)
- [skill-specs.md](.dev/releases/current/0.1/design/skill-specs.md): SKILL.md bodies for the 4 native/build-new skills + their resources (children.md/ya.md genre sources, agency ADR-003 rationale, source-fidelity v2.0 5-phase protocol + §3.2 output schema)
- [kb-formats.md](.dev/releases/current/0.1/design/kb-formats.md): tier_N.yaml schema §2 (+ schema-drift §2.2, T4 interpolation §2.4), adaptation-mapping cascade §3 (5-level resolution), per-tier canon graft G1 §4, shared-vs-per-tier split §5
- [boundary-contract.md](.dev/releases/current/0.1/design/boundary-contract.md): VENDOR.md manifest format §2, check_boundary.py verify algorithm rules A-F §3 (3 modes, body_of/frontmatter_line_diff helpers), enforcement points §4, upstream-sync protocol §5
- [safety-rubric.md](.dev/releases/current/0.1/design/safety-rubric.md): 6-section rubric with verbatim word lists, automatic failures §2, aggregation logic §3, machine-parseable verdict contract §4, FAIL→step-3 revision loop §4.1
- [tier-coordinator.md](.dev/releases/current/0.1/design/tier-coordinator.md): reconcile() 3 checks (A source-fidelity, B disclosure-leak, C monotonicity), parallel/sequential fan-out modes, cross-tier report format, chronicler ordering
- [GOAL/WHY](.dev/releases/current/0.1/design/DESIGN.md): implement LAF 0.1 (Path C Hybrid) end-to-end; the design pack is the settled buildable source of truth; Phase-3 hard gate = "0.1 done"

**Source areas:**
- **The design spec directory** `.dev/releases/current/0.1/design/` — the 7 [CODE-VERIFIED] specs that define every artifact (read for exact frontmatter, body protocols, formats, algorithms).
- **The LAF port-source configs** `config/age_profiles/`, `config/concept_mapping/`, `config/transformation_rules/`, `prompts/{analysis,verification,transformation}/`, `templates/` — carried VERBATIM into native kb/skill-resource targets (9 files, 716 lines, zero rework; only rename + `_`→`-`).
- **The upstream CWS checkout** (acquired Phase 0 from `https://github.com/haowjy/creative-writing-skills`, pinned at a chosen SHA) — provides the 23 adopted files vendored from `cw/agents/` + `cw/skills/` (Claude-lowered dialect, NOT Mars `agents/`).
- **The new `laf-adaptation/` tree** (greenfield — does not exist yet) — the single-tree distribution this task creates from scratch.

### Key Constraints
1. **ADR-006: prompt + YAML-config framework — NOT software.** Only ONE script (`scripts/check_boundary.py`, a validation tool not a runtime). Do not add other scripts or a CLI.
2. **Boundary contract #6 — native knowledge enters ADOPTED agents ONLY via `skills:` frontmatter, never body edits.** The one permitted vendor-time transform is the uniform prefix rewrite `creative-writing-skills:` → `laf-adaptation:`; `writer.md` additionally gets ONE additive `- laf-adaptation:adaptation-rules` line (ADOPTED-PATCHED). Preserve upstream quirks verbatim (e.g. writer.md's duplicate `creative-writing-craft` line).
3. **Carried-verbatim = zero rework for port-source.** Preserve schema drift (T1-T3 `conflict_to_cooperation`/`death_euphemism` vs T5 `conflict_handling`/`death_handling`); normalization belongs in reader skills (key-tolerant `.get(a) or .get(b)`), NOT in the vendored file.
4. **NO `tier_4.yaml`** — Tier 4 is interpolated at request time (T3 floor, T5 ceiling, conservative midpoint; agency_externalization = FORBIDDEN), never stored as a file. (A `resources/tier_4.md` *commentary* file IS authored — it is not a stored profile.)
5. **`<work>-mapping.yaml` has 5 top-level keys** (`work_metadata, characters, concepts, key_scenes, master_translation_table`) — spec §3.2 prose says "four" but the file + spec enumeration give FIVE (prose typo; build to 5).
6. **Tolkien is NOT public domain — do NOT commit real Tolkien prose.** The proof chapter is provisioned at execution time (external input by the operator, primary) or via a public-domain stand-in (committable fallback); seed the work-mapping from `config/concept_mapping/templates/tolkien_mapping.yaml` (a mapping config, not prose).
7. **QA intensity = full (PER_PHASE).** Lens-based QA gates (M3) after each major phase 0-3 + a final assembled-output gate; a SOURCE-DOCUMENT FIDELITY gate (M4/I21) after Phase 1 verifying carried YAML/skill-resource content is byte-faithful to its port-source. Every QA agent encoded as its own `- [ ]` item with a fully embedded lens-specific prompt. UV-only for all Python invocations (`uv run python scripts/check_boundary.py`).
8. **Reconciled counts (carry as authoritative — do NOT re-litigate the "13" or "6 agents" prose):** 15 agent files total (11 ADOPTED [5 dormant: brainstormer, outliner, character-sim, style-creator, web-researcher] + 2 NATIVE + 2 BUILD-NEW); 16 skill dirs (12 ADOPTED + 3 NATIVE + 1 BUILD-NEW); 23 adopted files vendored in Phase 0.

### Handoff File Convention

This task uses intra-task handoff patterns. Items write intermediate outputs to:
**`.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/`**

Subdirectories:
- `discovery/` - Discovery scan results and inventories (e.g. the upstream adopted-file enumeration)
- `test-results/` - Test output and summaries (check_boundary.py runs, Phase-3 11-step run captures)
- `reviews/` - Quality review verdicts (QA gate reports, Phase-3 gate-condition verdicts)
- `plans/` - Fix plans and conditional action outputs (safety FAIL loop, fix-cycle plans)
- `reports/` - Aggregated reports and summaries (phase-output summaries, consolidated Phase-3 gate report)

These files persist across all batches and session rollovers. Later items read them by path.

### Frontmatter Update Protocol

YOU MUST update the frontmatter at these MANDATORY checkpoints:
- **Upon Task Start:** Update `status` to "🟠 Doing" and `start_date` to current date
- **Upon Completion:** Update `status` to "🟢 Done" and `completion_date` to current date
- **If Blocked:** Update `status` to "⚪ Blocked" and populate `blocker_reason`
- **After Each Work Session:** Update `updated_date` to current date

DO NOT modify any other frontmatter fields unless explicitly directed by the user.

## Detailed Task Instructions

### Phase 1: Preparation and Setup

YOU MUST complete EVERY item in this checklist IN ORDER. DO NOT skip ahead. Mark each item as complete before proceeding to the next.

**Step 1.1:** Update task status
- [x] Update `status` to "🟠 Doing" and `start_date` to "2026-07-03" in the frontmatter of this task file, then add a timestamped entry to the ### Execution Log in the ## Task Log / Notes section at the bottom of this task file using the format: `**[YYYY-MM-DD HH:MM]** - Task started: Updated status to "🟠 Doing" and start_date.` Once done, mark this item as complete.

**Step 1.2:** Make the first git commit on `main` and repin `start_commit` (CRITICAL — unblocks the POST reflect gate)
- [x] CONTEXT: the repo on `main` currently has ZERO commits (all-untracked first-commit territory), so the task frontmatter `start_commit:` field (currently the sentinel `"no-commits-yet ..."`) is NOT a resolvable git ref. The POST reflect gate (Step PC.6) runs `superclaude reflect run ... --depth deep --fix --promote`, and the reflect wrapper resolves its audit base (the commit to diff against) from the task frontmatter `start_commit:` field; an unresolvable ref makes the wrapper return `Error: head-unresolved` (exit 2 = FAIL per the PC.6 contract), so the task could NEVER reach `status: 🟢 Done` against its declared starting condition. ACTION: (1) use the Bash tool to verify the zero-commit state via `git -C /config/workspace/Infantalizer rev-parse --verify HEAD` (expect failure / "unknown revision"), then (2) make the first commit on `main` for the CURRENT repo state — first ensure a `.gitignore` exists at repo root covering the upstream checkout dir chosen in Step 2.1 (recommended `.upstream-cws/` — confirmed present in `git status` as `?? .gitignore`) so the upstream CWS clone is NOT swept into this commit, then stage the current repo state with `git -C /config/workspace/Infantalizer add -A` (or a scoped add of the design pack + config/ + prompts/ + templates/ + this task file + docs/ — anything that establishes a parent commit), then commit with `git -C /config/workspace/Infantalizer commit -m "Initial commit: LAF design pack + port-source (pre-build baseline for TASK-RF-laf-hybrid-build-20260703-115948)"` (do NOT push; this is a local baseline commit only — the operator decides push timing), (3) re-capture the real SHA via `git -C /config/workspace/Infantalizer rev-parse HEAD` and capture it as a variable, then (4) update this task file's frontmatter `start_commit:` field via the Edit tool from the `"no-commits-yet ..."` sentinel to the captured 40-character SHA (e.g. `start_commit: "<40-char-SHA> (initial commit on main; reflect audit base)"`). OUTPUT: a real git commit on `main` plus the repinned `start_commit:` SHA written into this task file's frontmatter. VERIFICATION: re-run `git -C /config/workspace/Infantalizer rev-parse HEAD` and confirm it returns the same 40-char SHA now present in the frontmatter `start_commit:` field (byte-identical), and confirm `git -C /config/workspace/Infantalizer log --oneline -1` shows the commit on `main`. COMPLETION GATE: the `start_commit:` frontmatter value is a 40-char hex SHA resolvable by `git rev-parse`. Document that the reflect wrapper resolves its audit base from `start_commit:` so future readers understand WHY this repin is load-bearing. If the commit fails (e.g. git identity not configured), set `git config user.email`/`user.name` locally first and retry; if it fails for another reason, log the blocker in ### Phase 2 Findings and HALT (the POST reflect gate cannot run without a resolvable start_commit). Once done, mark this item as complete.

**Step 1.3:** Create handoff directories
- [x] Create the phase-outputs directory structure at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/` with subdirectories `discovery/`, `test-results/`, `reviews/`, `plans/`, and `reports/` to enable intra-task handoff between items (the discovery enumeration, check_boundary.py run captures, QA gate reports, fix-cycle plans, and gate aggregations all land here), ensuring all five subdirectories are created successfully. Once done, mark this item as complete.

### Phase 2: Phase 0 — Vendor (Acquire CWS, Vendor 23 Adopted Files, Boundary Contract)

**Gate:** `uv run python scripts/check_boundary.py --init --upstream <dir>` passes (exit 0); zero edits to vendored files apart from the deterministic prefix rewrite and the writer additive graft. The Phase-0 vendor set is **23 adopted files** = 11 adopted agents (10 ADOPTED-CLEAN + `writer.md` ADOPTED-PATCHED) + 12 adopted skills (each carrying its full `resources/**` subtree). All adopted files vendored from CWS **`cw/agents/`** and **`cw/skills/`** (already Claude-lowered), NEVER from `agents/` (Mars source with forbidden `model-policies`/`sandbox`/`effort`/`subagents` keys). See `boundary-contract.md §2-§3`, `DESIGN.md §2.1`, `agent-schemas.md §1`.

**Step 2.1:** Acquire upstream CWS checkout (L1 discovery — the discovery file IS the deliverable)
- [x] Read `boundary-contract.md §2` (lines 40-88) and `§3.1` (lines 94-104) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the upstream acquisition contract (the `--upstream <dir>` requirement, the `cw/agents/` vs `agents/` dialect rule, the VENDOR.md header fields `upstream_repo`/`upstream_sha`/`vendored_on`/`license`/`prefix_rewrite`), then use the Bash tool to clone the upstream repo `https://github.com/haowjy/creative-writing-skills` into a local checkout dir OUTSIDE `laf-adaptation/` (recommended `.upstream-cws/` at repo root, or a gitignored path so Rule F's `glob("agents/*.md")` does not scan it) and `git checkout <chosen-40-char-SHA>` in that checkout dir to pin the SHA (the exact SHA is an execution-time choice — pick the current upstream `main` HEAD or a recent stable commit), then use Glob and Read on the checkout's `cw/agents/` and `cw/skills/` subtrees to enumerate the actual upstream adopted files present at that SHA (confirm all 11 agents: muse, critic, editor, reader-sim, continuity-checker, brainstormer, outliner, character-sim, style-creator, web-researcher, writer; and all 12 skill dirs: writing-principles, creative-writing-craft, creative-writing-modes, story-review, story-memory, kb-management, shared-dao, llm-writing, writing-staffing, intent-modeling, grill-with-docs, creative-research, each with its SKILL.md and full `resources/**` subtree), then write a consolidated vendoring inventory to the file `upstream-cws-inventory.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` formatted as a markdown table with columns: Target Path (e.g. `agents/muse.md`, `skills/writing-principles/SKILL.md`, `skills/<name>/resources/<file>`), Upstream Path (the cw/ source), Provenance Class (ADOPTED-CLEAN or ADOPTED-PATCHED for writer), and a summary count at the top showing total agents (must be 11), total skill dirs (must be 12), and total recursive resource files hashed at the pinned SHA, ensuring every adopted file the manifest must hash is enumerated with no fabrication (paths come from the actual checkout Glob, not assumed), and the pinned SHA + checkout-dir path are recorded at the top of the inventory (they feed VENDOR.md `upstream_sha` and the `--upstream <dir>` flag). If the upstream clone fails (network/auth) or the `cw/agents/`+`cw/skills/` subtrees are absent at the chosen SHA, log the specific blocker using the templated format in the ### Phase 2 Findings section of the ## Task Log / Notes at the bottom of this task file, then mark this item complete. Once done, mark this item as complete.

**Step 2.2:** Author `scripts/check_boundary.py` (the ONLY script — required before `--init`)
- [x] Read `boundary-contract.md §3` (lines 92-167) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the FULL authorable spec — the three CLI modes (default `verify` exits 1 on any violation; `--init` writes manifest rows and REQUIRES `--upstream <dir>`; `--report` always exits 0), the verify algorithm rules A-F as verbatim pseudocode (A: laf-hash match for ADOPTED-CLEAN/PATCHED; B: ADOPTED-CLEAN == upstream-after-prefix-rewrite, needs upstream; C: ADOPTED-PATCHED body byte-identical + frontmatter additive-only, only `- laf-adaptation:` lines permitted, needs upstream; D: G3 quartet-intact for critic/editor/reader-sim/continuity-checker as ADOPTED-CLEAN and present, editor never folded; E: no NATIVE/BUILD-NEW name collides with upstream; F: every `glob("agents/*.md") + glob("skills/**/SKILL.md")` is in the manifest), and the helper definitions `body_of(text)` (everything after the second `---` delimiter; adopted bodies byte-for-byte) and `frontmatter_line_diff(a,b)` (line-level added/removed on the YAML frontmatter only after prefix normalization, ORDER-INSENSITIVE for `skills:` list entries — so writer's preserved duplicate `creative-writing-craft` line and the single additive `- laf-adaptation:adaptation-rules` line are handled correctly), then author the Python script `check_boundary.py` at `laf-adaptation/scripts/check_boundary.py` implementing: a VENDOR.md manifest parser (reads the 4-column table `path | class | upstream_sha256 | laf_sha256` + the 5 header fields + 3-line Invariants block), the deterministic `prefix_rewrite` function (`creative-writing-skills:` → `laf-adaptation:` recomputed, never trusted from a stored diff), the `verify()` function running all six rules A-F and collecting `errors[]` then `exit(1 if errors else 0)` with each error printed, the `--init` mode (requires `--upstream <dir>`: reads `upstream_blob(path)` from the checkout, computes both `upstream_sha256` and `laf_sha256` for adopted files, writes/updates the manifest rows; the writer additive graft is applied at VENDORING time by Step 2.3, NOT by `--init` — `--init` only HASHES the file Step 2.3 already produced, consistent with `boundary-contract.md:17-19` "never transforms the framework"), the `--report` mode (human-readable provenance summary, always exit 0), and sha256 computation that recursively hashes every file under each adopted skill's `resources/` (so a body edit to a resource is caught), ensuring the script is pure standard-library Python (no third-party deps — ADR-006 forbids a runtime; this is a validation tool), it never transforms the framework (only reads files and computes hashes per `boundary-contract.md:18-19`), that it ROOTS all relative paths from the repo root computed as `Path(__file__).resolve().parents[1]` (the `laf-adaptation/` parent) so the script is correct regardless of the caller's current working directory — the task invokes it from repo root via `uv run python laf-adaptation/scripts/check_boundary.py`, and relative globs like `glob("agents/*.md")` would otherwise resolve against the wrong base (`boundary-contract.md:109-150` specify relative globs; this rooting requirement makes them unambiguous), and rules B/C/E dereference `upstream_blob()`/`upstream_has()` against the `--upstream <dir>` only when provided (verify mode without `--upstream` uses recorded `upstream_sha256` + Rule A/D/F). If unable to complete due to an ambiguous spec point (e.g. whether verify re-fetches upstream), implement the safest reading (verify uses recorded hashes; full re-derivation at `--init`), document the decision in ### Phase 2 Findings, and proceed. Once done, mark this item as complete.

**Step 2.3:** Vendor the 11 adopted agents (per-file granularity — provenance grouped, manifest rows per-file)

*Note: each agent below is vendored from CWS `cw/agents/<name>.md` (Claude-lowered) with the uniform prefix rewrite `creative-writing-skills:` → `laf-adaptation:`. ADOPTED-CLEAN agents get ONE upstream_blob-after-rewrite hash (`upstream_sha256 == laf_sha256`). `writer.md` (the 11th) additionally appends ONE additive line `- laf-adaptation:adaptation-rules` (ADOPTED-PATCHED, two distinct hashes). Preserve upstream quirks verbatim (e.g. writer's duplicate `creative-writing-craft` line). Do NOT edit any body byte.*

- [x] Read the vendoring inventory `upstream-cws-inventory.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the 11 adopted agents and their upstream paths, then for the agent **muse** read the upstream file at `<checkout>/cw/agents/muse.md` to extract its content, then create the vendored file `muse.md` at `laf-adaptation/agents/muse.md` by copying the upstream content with ONLY the uniform prefix rewrite applied to every `creative-writing-skills:` occurrence in its frontmatter `skills:` list (rewriting each to `laf-adaptation:`), leaving the body byte-identical to upstream, ensuring NO body byte is changed (this is ADOPTED-CLEAN: `upstream_sha256 == laf_sha256` after prefix-normalization), NO Mars keys are introduced, and no content is fabricated beyond the upstream source. If unable to complete due to a missing upstream file, log the specific blocker in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **critic** read the upstream file at `<checkout>/cw/agents/critic.md`, then create the vendored file `critic.md` at `laf-adaptation/agents/critic.md` by copying upstream with ONLY the uniform prefix rewrite applied to its `skills:` list (ADOPTED-CLEAN), ensuring the body is byte-identical (this is one of the G3 quartet agents — it MUST be present and ADOPTED-CLEAN so Rule D passes), no Mars keys, no fabrication. If blocked by a missing upstream file, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **editor** read the upstream file at `<checkout>/cw/agents/editor.md`, then create the vendored file `editor.md` at `laf-adaptation/agents/editor.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical — this agent is the load-bearing G3 invariant ("editor.md is NEVER folded, NEVER modified"), so any body change FAILS the gate; do NOT add or remove any line. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **reader-sim** read the upstream file at `<checkout>/cw/agents/reader-sim.md`, then create the vendored file `reader-sim.md` at `laf-adaptation/agents/reader-sim.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical — the native persona payload is RUNTIME DATA emitted by the dispatcher, NOT a file edit (per `agent-schemas.md §4`), so do NOT graft any persona content into this file. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **continuity-checker** read the upstream file at `<checkout>/cw/agents/continuity-checker.md`, then create the vendored file `continuity-checker.md` at `laf-adaptation/agents/continuity-checker.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical (this is the 4th quartet agent — Rule D requires it present and ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **brainstormer** (dormant on the core adapt path but still vendored per the manifest) read the upstream file at `<checkout>/cw/agents/brainstormer.md`, then create the vendored file `brainstormer.md` at `laf-adaptation/agents/brainstormer.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical — dormant does NOT mean skipped; Rule F requires every `agents/*.md` to be manifest-listed, so this file MUST be vendored and hashed. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **outliner** (dormant) read the upstream file at `<checkout>/cw/agents/outliner.md`, then create the vendored file `outliner.md` at `laf-adaptation/agents/outliner.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **character-sim** (dormant) read the upstream file at `<checkout>/cw/agents/character-sim.md`, then create the vendored file `character-sim.md` at `laf-adaptation/agents/character-sim.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **style-creator** (dormant) read the upstream file at `<checkout>/cw/agents/style-creator.md`, then create the vendored file `style-creator.md` at `laf-adaptation/agents/style-creator.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the agent path, then for **web-researcher** (the CWS 11th agent; dormant on the core adapt path but vendored per the manifest) read the upstream file at `<checkout>/cw/agents/web-researcher.md`, then create the vendored file `web-researcher.md` at `laf-adaptation/agents/web-researcher.md` by copying upstream with ONLY the uniform prefix rewrite (ADOPTED-CLEAN), ensuring the body is byte-identical. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read `agent-schemas.md §3` (lines 126-184) at `.dev/releases/current/0.1/design/agent-schemas.md` to extract the canonical ADOPTED-PATCHED writer graft contract (the two mechanical transforms: (1) uniform prefix rewrite `creative-writing-skills:` → `laf-adaptation:` across all its `skills:` lines, and (2) the SINGLE additive line `- laf-adaptation:adaptation-rules` appended after the existing skill lines; the upstream duplicate `creative-writing-craft` line MUST be preserved verbatim and is NOT "fixed"; the body MUST be byte-identical to upstream), then read the upstream file at `<checkout>/cw/agents/writer.md` (path from the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`), then create the vendored file `writer.md` at `laf-adaptation/agents/writer.md` whose frontmatter `skills:` list is the upstream list with every `creative-writing-skills:` prefix rewritten to `laf-adaptation:` (preserving the duplicate `creative-writing-craft` entry) PLUS exactly one appended line `- laf-adaptation:adaptation-rules`, and whose body (everything after the closing `---`) is byte-identical to upstream, ensuring the diff vs upstream-after-prefix-rewrite is FRONTMATTER-ONLY and ADDITIVE (Rule C: `body_of(cur) == body_of(up)`, no `removed` lines, every `added` line starts with `- laf-adaptation:`), so this file stores TWO distinct hashes (`upstream_sha256` of the original cw file, `laf_sha256` of the vendored-with-graft file) and is classed ADOPTED-PATCHED. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.4:** Vendor the 12 adopted skill directories (per-skill granularity — each skill = SKILL.md + full `resources/**` subtree, all prefix-rewritten)

*Note: each adopted skill is vendored from CWS `cw/skills/<name>/` with the uniform prefix rewrite applied to its SKILL.md and every file under `resources/` (recursively hashed). The exact per-skill `resources/**` file list is whatever the pinned upstream ships — discover it from the checkout, do NOT assume.*

- [x] Read the vendoring inventory `upstream-cws-inventory.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md` to identify the skill and its resource files, then for **writing-principles** read the upstream SKILL.md at `<checkout>/cw/skills/writing-principles/SKILL.md` and use Glob to enumerate every file under `<checkout>/cw/skills/writing-principles/resources/**`, then create the vendored skill directory at `laf-adaptation/skills/writing-principles/` containing the SKILL.md and the full `resources/` subtree copied verbatim with ONLY the uniform prefix rewrite `creative-writing-skills:` → `laf-adaptation:` applied (every file — SKILL.md AND each resource — is ADOPTED-CLEAN: byte-identical to upstream-after-rewrite), ensuring NO Mars keys (`type`, `model-invocable`, `effort`) survive (the cw-lowered dialect already excludes them), every resource file present upstream is copied (none silently dropped), and every file is recursively hashable so Rule B catches any later body edit. If blocked by a missing upstream skill dir, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **creative-writing-craft** read the upstream SKILL.md at `<checkout>/cw/skills/creative-writing-craft/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/creative-writing-craft/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN), ensuring all upstream resources are copied verbatim and no Mars keys survive. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **creative-writing-modes** read the upstream SKILL.md at `<checkout>/cw/skills/creative-writing-modes/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/creative-writing-modes/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **story-review** read the upstream SKILL.md at `<checkout>/cw/skills/story-review/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/story-review/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **story-memory** read the upstream SKILL.md at `<checkout>/cw/skills/story-memory/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/story-memory/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **kb-management** read the upstream SKILL.md at `<checkout>/cw/skills/kb-management/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/kb-management/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **shared-dao** read the upstream SKILL.md at `<checkout>/cw/skills/shared-dao/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/shared-dao/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **llm-writing** read the upstream SKILL.md at `<checkout>/cw/skills/llm-writing/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/llm-writing/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **writing-staffing** read the upstream SKILL.md at `<checkout>/cw/skills/writing-staffing/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/writing-staffing/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **intent-modeling** read the upstream SKILL.md at `<checkout>/cw/skills/intent-modeling/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/intent-modeling/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **grill-with-docs** read the upstream SKILL.md at `<checkout>/cw/skills/grill-with-docs/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/grill-with-docs/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.
- [x] Read the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then for **creative-research** read the upstream SKILL.md at `<checkout>/cw/skills/creative-research/SKILL.md` and Glob its `resources/**`, then create the vendored skill at `laf-adaptation/skills/creative-research/` (SKILL.md + full resources subtree, prefix-rewritten, ADOPTED-CLEAN), ensuring all 12 adopted skill dirs are now vendored. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.5:** Create the adopted `kb/` scaffold (C1-C7)
- [x] Read `DESIGN.md §2` (lines 132-140) and `kb-formats.md §1` (lines 9-39) at `.dev/releases/current/0.1/design/` to extract the adopted kb scaffold layout (the CWS kb container: `kb/canon/`, `kb/characters/`, `kb/world/`, `kb/timeline/`, `kb/styles/`, `kb/issues/` directories plus the `kb/vocab.md` file — 6 dirs + 1 file = C1-C7), then create these scaffold dirs and the empty `vocab.md` placeholder at `laf-adaptation/kb/` so the adopted lifecycle machinery (chronicler/kb-management) has its container at framework build time, ensuring these are scaffold-only (empty dirs / placeholder — they are populated at runtime by adopted machinery, NOT vendored as fixed content per `kb-formats.md:11-12`), and no content is fabricated beyond the placeholder. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.6:** Create the adopted `work/` scaffold (drafts + critique-reports)
- [x] Read `DESIGN.md §2` (lines 127-131) at `.dev/releases/current/0.1/design/DESIGN.md` to extract the adopted `work/` lifecycle dirs (`work/drafts/` for the writer output and `work/critique-reports/` for the critic output — the adopted lifecycle containers; the native subdirs `work/analysis/` and `work/safety-reports/` are added in Phase 1), then create these two scaffold dirs at `laf-adaptation/work/drafts/` and `laf-adaptation/work/critique-reports/`, ensuring they are empty scaffolds (runtime-populated by the adopted writer/critic agents). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.7:** Author `CLAUDE.md` (adopted pattern + LAF conventions section + boundary-contract note)
- [x] Read `DESIGN.md` line 105 and §1 (lines 38-51) and §2.1 (lines 147-154) and §5 (lines 264-273) at `.dev/releases/current/0.1/design/DESIGN.md` to extract the CLAUDE.md contract ("ADOPTED pattern + LAF conventions section + boundary-contract note", the provenance model, the Claude-lowered dialect, the 6 constraints), then read the upstream CWS `CLAUDE.md` at `<checkout>/CLAUDE.md` to confirm the adopted "pattern" is a thin entry-point that references a fuller conventions doc (it is literally `@AGENTS.md`), then author `CLAUDE.md` at `laf-adaptation/CLAUDE.md` as NATIVE-authored prose adopting the thin-entry-point PATTERN (NOT the file — LAF has no AGENTS.md and its conventions differ: the Mars/Meridian layer is cut) containing the four required parts: (1) a Provenance Model section (ADOPTED / NATIVE / BUILD-NEW — never edit an adopted body, see VENDOR.md), (2) the Boundary Contract section (constraint #6 — native knowledge enters ADOPTED agents ONLY via `skills:` frontmatter; enforcement `uv run python scripts/check_boundary.py`; only writer.md is ADOPTED-PATCHED, additive `- laf-adaptation:<skill>` line only), (3) the LAF Conventions section (Claude-native/cw-lowered frontmatter — `name`+`description` for skills, Claude agent keys for agents, NO Mars keys; tier axis first-class with `active_tier` explicit param and T4 interpolated-never-stored; the 6 constraints; UV-only for Python; one flat tree), and (4) a "Where things live" section (15 agents, 16 skills, kb/tiers + kb/adaptation-mapping + kb/adaptations, work/ lifecycle, scripts/check_boundary.py the ONLY script, VENDOR.md + NOTICE), ensuring the file states its provenance line ("Structure adopted from upstream CWS CLAUDE.md/AGENTS.md convention; content authored for LAF (Mars/Meridian layer removed)"), it is NOT hash-pinned (outside the Rule-F glob), and no content is fabricated beyond the cited spec sections. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.8:** Author `NOTICE` (Apache-2.0 attribution — required committed file)
- [x] Read `boundary-contract.md` line 48 at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the attribution contract (`license: Apache-2.0 (upstream) — attribution retained per NOTICE`), then read the upstream `LICENSE` at `<checkout>/LICENSE` to confirm upstream is Apache-2.0 (and ships NO NOTICE file — verified), then author `NOTICE` at `laf-adaptation/NOTICE` as NATIVE-authored attribution prose carrying: the project name (LAF Adaptation Framework / laf-adaptation), a statement that the distribution contains files vendored unmodified (apart from the deterministic prefix rewrite) from `creative-writing-skills` (upstream repo URL `https://github.com/haowjy/creative-writing-skills`, vendored at commit `<upstream_sha from VENDOR.md>`, vendored on `<date>`), a statement that the vendored files are Apache-2.0 licensed with a copy accompanying (see `LICENSE-CWS`) and a link to `http://www.apache.org/licenses/LICENSE-2.0`, a copyright/attribution line for the upstream authors ("the creative-writing-skills authors" unless a named holder is found in the upstream repo), and a statement that NATIVE/BUILD-NEW files are authored by LAF and NOT covered by upstream attribution, ensuring the file is placed at `laf-adaptation/NOTICE` (NOT repo root — the repo root is MIT, the upstream is Apache-2.0, so co-locate with VENDOR.md/LICENSE-CWS inside laf-adaptation/), it is NOT hash-pinned (outside the Rule-F glob), and the `<upstream_sha>` placeholder is left for VENDOR.md to fill. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.9:** Vendor the upstream Apache-2.0 `LICENSE` as `LICENSE-CWS`
- [x] Read the upstream `LICENSE` at `<checkout>/LICENSE` to extract the full Apache-2.0 license text (Apache §4(a) requires distributing a copy of the License with the Work), then create the vendored license copy `LICENSE-CWS` at `laf-adaptation/LICENSE-CWS` containing the full Apache-2.0 text verbatim (so the full license text travels with the NOTICE inside the adopted subtree), ensuring the filename is `LICENSE-CWS` (not `LICENSE`, to avoid collision with any future `laf-adaptation/LICENSE` and to make clear it covers the CWS-vendored subset) and no content is altered. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.10:** Author `.githooks/pre-commit` (opt-in enforcement wrapper)
- [x] Read `boundary-contract.md §4` (lines 171-181) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the enforcement contract (the pre-commit hook is opt-in per clone, blocking — blocks the commit on failure; UV-wrapped invocation `uv run python scripts/check_boundary.py`), then read the upstream `.githooks/pre-commit` at `<checkout>/.githooks/pre-commit` to confirm the adopted opt-in hook pattern (`set -uo pipefail`, runs checks, `exit 1` on failure, documents `git config core.hooksPath .githooks` enable and `--no-verify` bypass), then author the pre-commit hook at `laf-adaptation/.githooks/pre-commit` as a bash script with `set -uo pipefail` that runs `uv run python scripts/check_boundary.py` and `exit 1` on failure with a clear stderr message, including the opt-in enable instructions (`git config core.hooksPath .githooks`) and bypass instructions (`git commit --no-verify`) as comments, then use the Bash tool to `chmod +x laf-adaptation/.githooks/pre-commit` to mark it executable, ensuring the hook `cd`s to the laf-adaptation dir first if it is nested below the git root (the repo root here is `/config/workspace/Infantalizer`, so if `laf-adaptation/` is a subdir the hook must `cd "$(git rev-parse --show-toplevel)/laf-adaptation"` — confirm placement at execution time), and no content is fabricated beyond the cited spec. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.11:** Author the CI workflow `.github/workflows/boundary.yml`
- [x] Read `boundary-contract.md §4` (lines 173-181) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the CI contract (the PR gate is blocking — fails the PR; UV-wrapped invocation), then use the Bash tool to verify the repo-root `.github/` is empty (no `workflows/` dir exists — confirmed, must be created), then create the CI workflow file `boundary.yml` at `.github/workflows/boundary.yml` (creating the `.github/workflows/` directory first) as a GitHub Actions workflow with `name: Boundary Contract`, `on: [push to main, pull_request to main]`, a `check-boundary` job on `ubuntu-latest` using `actions/checkout@v4` + `astral-sh/setup-uv@v7` (pinned to v7 for consistency with the adopted CWS CI) and a step `uv run python scripts/check_boundary.py` (verify mode needs NO `--upstream` checkout — rules A/D/F work on the vendored tree + manifest alone), ensuring the `working-directory:` is set to `laf-adaptation` ONLY if `laf-adaptation/` is nested below the git root (it is here — `/config/workspace/Infantalizer/laf-adaptation`), the workflow is the ONLY CI step this framework ships (ADR-006 — no test/build matrix, just the boundary gate), and no content is fabricated beyond the cited spec. If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.12:** Author the `VENDOR.md` header + Invariants block
- [x] Read `boundary-contract.md §2` (lines 40-88) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the exact VENDOR.md format (5 header fields: `upstream_repo` literal `https://github.com/haowjy/creative-writing-skills`, `upstream_sha` = the pinned 40-char SHA from the vendoring inventory at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, `vendored_on` = 2026-07-03, `license` = `Apache-2.0 (upstream) — attribution retained per NOTICE`, `prefix_rewrite` = `"creative-writing-skills:" -> "laf-adaptation:"`; and the 3-line Invariants block asserted by check_boundary.py: G3 quartet-intact for critic/editor/reader-sim/continuity-checker as ADOPTED-CLEAN, editor.md NEVER folded/modified, no NATIVE/BUILD-NEW name collides with upstream), then author the file `VENDOR.md` at `laf-adaptation/VENDOR.md` containing the 5 header fields (filling `upstream_sha` with the SHA pinned in Step 2.1), the full Invariants block, and a `## Manifest` section header followed by the 4-column table header (`| path | class | upstream_sha256 | laf_sha256 |`) — the actual manifest ROWS are populated by the `--init` run in Step 2.13, ensuring the header is byte-faithful to `boundary-contract.md §2` and no hashes are hand-authored (they are computed by the script). If blocked, log in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.13:** Run `check_boundary.py --init` to populate the manifest (L3 test/execute — captures the boundary gate result)
- [x] Use the Bash tool to run the command `uv run python laf-adaptation/scripts/check_boundary.py --init --upstream <checkout-dir>` (substituting the checkout dir pinned in Step 2.1) and capture the complete output, then write the raw output to the file `boundary-init-output.txt` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/boundary-init-output.txt` preserving the exact output, then create a structured summary file `boundary-init-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/boundary-init-summary.md` containing: overall result (PASSED if exit 0 / FAILED otherwise), the exit code, the count of manifest rows written (must be ≥23 adopted agent+skill rows plus their recursive resource hashes), confirmation that VENDOR.md `upstream_sha` and `vendored_on` were recorded, and any errors printed, ensuring the summary accurately reflects the raw output with no fabricated results. IF the result is PASSED (exit 0), proceed. IF the result is FAILED (exit 1, typically a Rule A/B/C/D/E/F violation), read the raw output to identify the failing rule and the offending file, fix the vendoring (re-copy the offending file from upstream with the correct prefix rewrite / additive graft), and re-run `--init` until exit 0 (max 3 fix cycles — if still failing after 3 cycles, HALT and log the unresolved violations in ### Phase 2 Findings). If the command fails to execute (missing uv, missing checkout dir, script syntax error), log the specific blocker in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 2.14:** Phase-0 boundary gate verification (L3 test/execute — final green confirmation)
- [x] Use the Bash tool to run the command `uv run python laf-adaptation/scripts/check_boundary.py` (default verify mode, NO `--upstream`) and capture the complete output, then write the raw output to the file `boundary-verify-phase0-output.txt` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/boundary-verify-phase0-output.txt` and a structured summary `boundary-verify-phase0-summary.md` at the same dir containing: overall result (PASSED if exit 0), exit code, the per-rule pass/fail (rules A-F), and confirmation that zero edits exist beyond the deterministic prefix rewrite + writer additive graft, ensuring the summary accurately reflects the raw output. The gate is GREEN only if exit 0 — this means: (A) every ADOPTED file matches its recorded `laf_sha256`, (B) every ADOPTED-CLEAN file == upstream-after-prefix-rewrite, (C) `writer.md` diff is frontmatter-only + additive, (D) the quartet {critic,editor,reader-sim,continuity-checker} is intact and ADOPTED-CLEAN, (E) no NATIVE/BUILD-NEW file yet exists to collide (Phase 0 has only adopted files), (F) every `agents/*.md` + `skills/**/SKILL.md` is in the manifest. IF exit 0, the Phase-0 boundary gate PASSES — proceed. IF exit 1, log the specific rule failure in ### Phase 2 Findings, fix, and re-run (max 3 cycles). If the command fails to execute, log the blocker in ### Phase 2 Findings, then mark this item complete. Once done, mark this item as complete.

### Phase Gate 0: Quality Verification (M3 Lens-Based QA)

**Step PG0.1:** Aggregate Phase-0 outputs (L6)
- [x] Use Glob to find all Phase-0 output files matching `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/boundary-*.md` and `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/discovery/upstream-cws-inventory.md`, then read each to extract the vendoring result (23 adopted files vendored, manifest row count, boundary gate exit code), then create the consolidated Phase-0 output summary at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/phase-0-output-summary.md` containing: an executive summary (vendored 11 agents + 12 skills = 23 files, check_boundary.py --init + verify both exit 0, VENDOR.md manifest populated), a table of vendored artifacts with columns Path, Provenance Class, Vendored-From, a list of any blockers encountered, and an overall Phase-0 verdict (READY FOR PHASE 1 if boundary gate green), ensuring the report accurately aggregates the captured outputs with no fabricated statistics. If no output files are found by Glob, log this as a blocker in ### Phase Gate Findings, then mark this item complete. Once done, mark this item as complete.

**Step PG0.2:** Spawn structural lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa agent with the template-conformance lens to review the Phase-0 vendored artifacts (the 11 agents, 12 skills, VENDOR.md, NOTICE, LICENSE-CWS, CLAUDE.md) against `boundary-contract.md §2-§3` and `agent-schemas.md §1`, checking that every adopted SKILL.md/agent.md uses ONLY the closed Claude-lowered key set (`name`, `description`, `model` for agents, `skills` as fully-qualified `laf-adaptation:<skill>`, `tools`) with NO Mars keys (`type`, `model-invocable`, `effort`, `model-policies`, `sandbox`, `subagents`), that VENDOR.md has the exact 5 header fields + 3-line Invariants block + 4-column manifest table, and that no placeholder/sentinel text remains, with adversarial framing "Assume this vendored tree has at least 5 errors in template conformance (a smuggled Mars key, a malformed manifest row, a placeholder hash). Find them.", producing the report `qa-structural-template-conformance-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-structural-template-conformance-report-phase0.md` with `fix_authorization: false` (report only). Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the internal-consistency lens to review the Phase-0 artifacts, checking that the vendoring inventory count (23 adopted files) matches the VENDOR.md manifest row count, that the pinned `upstream_sha` in VENDOR.md matches the SHA recorded in the discovery inventory, that `vendored_on` is a valid ISO date, that the prefix_rewrite declaration is consistent across VENDOR.md/NOTICE/CLAUDE.md, and that the quartet agents {critic,editor,reader-sim,continuity-checker} are all present and classed ADOPTED-CLEAN, with adversarial framing "Assume this Phase-0 output has at least 5 internal inconsistencies (a count mismatch, a SHA typo, a missing quartet row). Find them.", producing the report `qa-structural-internal-consistency-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-structural-internal-consistency-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the evidence-quality lens to review the Phase-0 artifacts, checking that every claim in VENDOR.md/NOTICE/CLAUDE.md cites a verifiable source (the pinned upstream checkout, the spec line, the computed hash), that the boundary-init-summary and boundary-verify-phase0-summary report the actual exit codes (not assumed), that no hallucinated upstream paths or SHAs appear, and that the LICENSE-CWS text matches the upstream Apache-2.0 LICENSE byte-for-byte, with adversarial framing "Assume at least 5 evidence-quality errors (an assumed hash, a fabricated SHA, an unverified license claim). Find them.", producing the report `qa-structural-evidence-quality-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-structural-evidence-quality-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the boundary-contract-fidelity lens to run the actual enforcement gate: use the Bash tool to execute `uv run python laf-adaptation/scripts/check_boundary.py` and verify exit 0, then independently confirm rules A-F hold by spot-checking 3 adopted files (recompute sha256, confirm `upstream_sha256 == laf_sha256` for an ADOPTED-CLEAN agent, confirm the writer diff is frontmatter-only+additive via a manual diff, confirm the quartet is intact), with adversarial framing "Assume the boundary gate has at least 5 latent violations (an adopted body byte-change, a missing manifest row, a writer body edit). Find them by running the script AND manual spot-checks.", producing the report `qa-structural-boundary-fidelity-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-structural-boundary-fidelity-report-phase0.md` with `fix_authorization: false`. (Note: standardized to rf-qa-qualitative per fix-round-1 Q-3 — the boundary-contract-fidelity lens verifies content/correctness of the boundary contract across ALL 5 gates: PG0/PG1/PG2/PG3/PC.) Once done, mark this item as complete.

**Step PG0.3:** Spawn content lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa-qualitative agent with the actionability lens to review CLAUDE.md and VENDOR.md, checking that every instruction is specific enough to execute without interpretation (e.g. "if you think you must edit an adopted file: STOP. Add a skill instead" is actionable; vague guidance is not), that the upstream-sync and enforcement pointers name exact commands, and that the provenance model lets a contributor unambiguously classify any new file, with adversarial framing "Assume at least 5 actionability gaps (an ambiguous instruction, a missing command, an unclassifiable edge case). Find them.", producing the report `qa-content-actionability-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-content-actionability-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the source-fidelity lens to verify the vendored adopted files are byte-faithful to the pinned upstream after prefix rewrite, reading BOTH the upstream checkout files AND the vendored laf-adaptation files and confirming (a) each ADOPTED-CLEAN agent/skill body is byte-identical to upstream-after-prefix-rewrite, (b) the writer additive graft is the ONLY frontmatter delta, and (c) LICENSE-CWS matches upstream LICENSE verbatim, with adversarial framing "Assume at least 5 fidelity breaches (a 'fixed' upstream quirk, a silent body edit, a dropped resource file). Find them by direct byte comparison.", producing the report `qa-content-source-fidelity-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-content-source-fidelity-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the domain-accuracy lens to verify the vendored tree matches the design intent, reading `DESIGN.md §2` and confirming the distribution layout (agents/, skills/, kb/, work/, scripts/, root files) matches the spec, that the 11 adopted agents + 12 adopted skills are the ones named in `DESIGN.md §1.1`, that chronicler is NOT among the vendored (it is build-new in Phase 2 — unshipped vapor in both systems per `DESIGN.md §1.1`), and that web-researcher IS vendored (the CWS 11th agent), with adversarial framing "Assume at least 5 domain-accuracy errors (a wrong agent count, a missing web-researcher, a wrongly-vendored chronicler). Find them.", producing the report `qa-content-domain-accuracy-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-content-domain-accuracy-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG0.4:** Consolidate findings and apply fixes (serialized per I20)
- [x] Read all 7 QA reports from Steps PG0.2-PG0.3 (the 4 structural + 3 content lens reports in `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-structural-*.md` and `qa-content-*.md`), consolidate them into a single findings file `qa-consolidated-findings.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-consolidated-findings.md` listing every issue from every agent, deduplicated (the same issue found by multiple lenses listed once with all originating lenses noted), each tagged with severity (CRITICAL/IMPORTANT/MINOR) and the originating lens, ensuring the consolidation faithfully represents all reports with no omitted issues. Once done, mark this item as complete.
- [x] Spawn ONE rf-qa fix agent with `fix_authorization: true` and the consolidated findings file `qa-consolidated-findings.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-consolidated-findings.md` as input, instructing it to apply ALL consolidated fixes to the Phase-0 vendored artifacts (VENDOR.md, NOTICE, CLAUDE.md, and re-vendoring any offending adopted file from upstream with the correct transform — never editing an adopted body beyond the permitted transforms), producing a fix-applied summary. This is the ONLY agent authorized to modify the artifacts in this gate (serialized fix protocol per I20 — parallel fix authorization is PROHIBITED). Once done, mark this item as complete.

**Step PG0.5:** Verification round (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa verification agent to confirm all consolidated findings were addressed and no new issues introduced by the fixes, re-running `uv run python laf-adaptation/scripts/check_boundary.py` to confirm the boundary gate is still green (exit 0) after the fixes, producing the report `qa-verification-structural-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-verification-structural-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative verification agent to confirm content quality was maintained after the fixes (no introduced Mars keys, no broken manifest rows, no lost attribution), producing the report `qa-verification-content-report-phase0.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-verification-content-report-phase0.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Read the two verification reports. IF both report PASS, the Phase-0 QA gate PASSES — proceed to Phase 1. IF either reports FAIL, repeat Steps PG0.4-PG0.5 (consolidate new + remaining findings, fix, verify) up to a max of 3 fix cycles (report-validation gate type per I16); if issues remain after 3 cycles, HALT and escalate, logging the unresolved issues in ### Phase Gate Findings. Once done, mark this item as complete.

### Phase 3: Phase 1 — Native Spine (3 Native Skills, 2 Native Agents, Writer Patch, kb Ports)

**Gate:** native skills validate (Claude-native frontmatter, NO Mars keys); `writer.md` diff is frontmatter-only AND additive (Rule C); ported kb YAMLs are byte-faithful to LAF port-source (zero rework); `check_boundary.py` stays green with the native files added to the manifest. See `DESIGN.md §7 Phase 1`, `skill-specs.md §1-§3`, `agent-schemas.md §2`, `kb-formats.md §2-§3`.

**Step 3.1:** Author `skills/adaptation-tiers/SKILL.md` (NATIVE)
- [x] Read `skill-specs.md §1` (lines 33-83) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the exact adaptation-tiers SKILL.md content — the frontmatter (`name: adaptation-tiers`, multi-line `description: |` block stating the five-tier developmental adaptation axis grounded in Piaget/Kohlberg and the explicit `active_tier` parameter) and the 4-section body verbatim (the tier table 1-5 with ages/Piaget/Kohlberg/paradigm; "Loading a tier profile" instructing to read `kb/tiers/tier_<N>.yaml`; "The transformation-mode ladder (cross-cutting)" with the MANDATORY→OPTIONAL→FORBIDDEN agency ladder; "Interpolation (Tier 4)" with the T3-floor/T5-ceiling/conservative-midpoint rule and `agency_externalization = FORBIDDEN`), then create the file `SKILL.md` at `laf-adaptation/skills/adaptation-tiers/SKILL.md` containing the frontmatter (Claude-native `name`+`description` ONLY, NO Mars keys `type`/`model-invocable`/`effort`) and the 4-section body verbatim, ensuring the body is byte-faithful to `skill-specs.md §1` (no fabricated tiers, no paraphrased interpolation rule), it instructs reading `kb/tiers/tier_<N>.yaml` at runtime but does NOT embed the YAML data (that is R4's ported data), and no `rules/` or `templates/` subdirs are created (they don't exist in CWS). If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.2:** Author the 5 adaptation-tiers tier commentary resources (`tier_1.md` … `tier_5.md`)
- [x] Read `skill-specs.md §1` (lines 80-83) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the commentary sourcing contract ("design commentary carried from `docs/design_decisions/001-five-tier-system.md` and `003-agency-externalization.md` — the rationale, NOT the data"), then read `docs/design_decisions/001-five-tier-system.md` and `docs/design_decisions/003-agency-externalization.md` at `/config/workspace/Infantalizer/docs/design_decisions/` to extract the developmental/threshold RATIONALE for each tier (e.g. "externalization prevents self-blame per Piagetian egocentrism"), then create 5 commentary files: `tier_1.md`, `tier_2.md`, `tier_3.md`, `tier_5.md` at `laf-adaptation/skills/adaptation-tiers/resources/` each carrying the rationale for that stored tier from design_decisions 001 & 003, AND `tier_4.md` at the same dir carrying the interpolation-derivation rationale (T3 floor, T5 ceiling, midpoint rounding toward conservative, `agency_externalization` inherits T4_5=FORBIDDEN) sourced from the SKILL.md §Interpolation block + the T3/T5 profiles it interpolates between, ensuring tier_4.md explains the DERIVATION and does NOT define a stored profile or numeric constant set as if canonical (it must not re-introduce a de-facto tier_4 profile), each file holds REASONING (not data — the data lives in `kb/tiers/*.yaml`), and content is derived from the cited design-decision docs with no fabrication. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.3:** Author `skills/adaptation-rules/SKILL.md` (NATIVE)
- [x] Read `skill-specs.md §2` (lines 87-160) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the exact adaptation-rules SKILL.md content — the frontmatter (`name: adaptation-rules`, multi-line `description: |` block covering thematic/character transformation keyed by tier including Agency Externalization) and the body verbatim ("# Adaptation Rules"; the resources list pointing to `resources/thematic.md`, `resources/character.md`, `resources/agency.md`; "Applying a rule" 4-step procedure; "Cascade with work mappings" noting most-specific-wins precedence), then create the file `SKILL.md` at `laf-adaptation/skills/adaptation-rules/SKILL.md` containing the frontmatter (Claude-native `name`+`description` ONLY) and the body verbatim, ensuring it includes the key-tolerant lookup note (the consuming reader must handle the conflict/death key drift via `.get(a) or .get(b)` — this belongs in the skill body per `kb-formats.md §2.2`, not in the kb file) and no Mars keys. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.4:** Author `skills/adaptation-rules/resources/thematic.md` (fenced verbatim port of thematic.yaml)
- [x] Read `config/transformation_rules/thematic.yaml` at `/config/workspace/Infantalizer/config/transformation_rules/thematic.yaml` to extract the FULL transformation-rules YAML verbatim (the 6 rule groups: `violence_transformation`, `conflict_transformation`, `agency_externalization`, `death_handling`, `villain_motivation`, `heroism_definition`, each with `tier_1`/`tier_2`/`tier_3`/`tier_4_5` buckets and `mode:` enums), then read `skill-specs.md §2.1` (lines 128-133) to confirm the wrapper contract (this resource WRAPS the thematic.yaml in a fenced ```yaml block, unchanged — no key renamed, this is the CANONICAL rule source per `kb-formats.md §2.3`), then create the file `thematic.md` at `laf-adaptation/skills/adaptation-rules/resources/thematic.md` containing the thematic.yaml content inside a single ```yaml fenced block copied BYTE-FOR-BYTE from the source (zero rework — do NOT normalize the `death_handling` key or any other drift; the YAML is carried verbatim), preceded by a one-line header noting it is the canonical thematic transformation rule source carried verbatim from `config/transformation_rules/thematic.yaml`, ensuring the fenced content is byte-identical to the source file and no key is renamed or value altered. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.5:** Author `skills/adaptation-rules/resources/character.md` (fenced verbatim port of character.yaml)
- [x] Read `config/transformation_rules/character.yaml` at `/config/workspace/Infantalizer/config/transformation_rules/character.yaml` to extract the FULL archetype-system YAML verbatim (`archetype_system`, `tier_1_archetypes`, `tier_2_additions` (rival), `tier_3_unlocks` (hero_with_flaws/complex_villain/morally_ambiguous), `tier_4_5` (`approach: preserve_original`), the `heroism_translation.subroutine` 3-step IDENTIFY/LOOKUP/TRANSLATE block scalar, the `heroism_translation.examples` keyed by phrase with tier buckets, and `special_handling` per-character overrides (gollum, denethor)), then read `skill-specs.md §2.2` (lines 135-141) to confirm the wrapper contract (WRAPS character.yaml in a fenced block, unchanged, ported as an executable procedure), then create the file `character.md` at `laf-adaptation/skills/adaptation-rules/resources/character.md` containing the character.yaml content inside a single ```yaml fenced block copied BYTE-FOR-BYTE from the source, preceded by a one-line header noting it is the character archetype + heroism-translation source carried verbatim from `config/transformation_rules/character.yaml`, ensuring the fenced content is byte-identical (the literal 3-step subroutine preserved verbatim) and no key renamed. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.6:** Author `skills/adaptation-rules/resources/agency.md` (Agency Externalization + ADR-003 rationale)
- [x] Read `skill-specs.md §2.3` (lines 143-160) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the Agency Externalization content — the sample YAML shape (`agency_externalization` with `core_principle`, `tier_1` mandatory examples, `tier_2` mandatory softening, `tier_3` optional, `tier_4_5` forbidden) AND the ADR-003 rationale prose (it lives in the native `/adaptation-rules` skill NOT bolted onto the adopted `writing-principles`, because grafting native content onto an adopted skill would break the boundary contract; it is loaded additively by adopted writer/critic; enforced by writer, audited by safety-verifier rubric Section 2 and critic), then read `config/transformation_rules/thematic.yaml` (the `agency_externalization` block at line ~33) and `docs/design_decisions/003-agency-externalization.md` to source the verbatim agency rule + the rationale, then create the file `agency.md` at `laf-adaptation/skills/adaptation-rules/resources/agency.md` containing the agency_externalization YAML inside a fenced ```yaml block (carried verbatim from thematic.yaml's `agency_externalization` section) PLUS the ADR-003 rationale prose explaining WHY it lives in the native skill (boundary contract) and how it is enforced, ensuring the YAML is byte-faithful to thematic.yaml's agency block and the rationale is derived from `skill-specs.md §2.3` + design_decisions 003 with no fabrication. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.7:** Author `skills/source-fidelity/SKILL.md` (NATIVE — v2.0 protocol + output schema)
- [x] Read `skill-specs.md §3` (lines 164-247) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the exact source-fidelity SKILL.md content — the frontmatter (`name: source-fidelity`, multi-line `description: |` block covering the v2.0 anti-hallucination protocol, CERTAIN/PROBABLE/UNCERTAIN tags, source-access declaration with ABORT-on-NO-ACCESS), the 5-phase protocol body verbatim ("# Source Fidelity — v2.0 Pragmatic Verification Protocol"; the confidence-tags table; "Phase 0 — Source Declaration" with FULL/PARTIAL/MEMORY-BASED/NO ACCESS and the ABORT hard gate; "Phase 1 — Essential Verification"; "Phase 2 — Dual-Pass Documentation" PASS ONE/TWO; "Phase 3 — Transformation Flags"; "Phase 4 — Consistency Check"), and the §3.2 output schema (`work/analysis/ch-<NN>.yaml` with `status`, `metadata`, `essentials`, `characters`, `events`, `summary`, `transformation_flags`, `uncertainties`), then read `prompts/analysis/chapter_analysis.md` at `/config/workspace/Infantalizer/prompts/analysis/chapter_analysis.md` to confirm the carried prompt content, then create the file `SKILL.md` at `laf-adaptation/skills/source-fidelity/SKILL.md` containing the frontmatter (Claude-native) + the 5-phase body verbatim + the §3.2 output YAML schema documented so the analyst agent and the skill agree on the contract, ensuring the ABORT-on-NO-ACCESS hard gate is present verbatim (constraint #2), the tag-propagation note (tags flow through the adopted story-memory carrier with NO edit to story-memory) is included, and no Mars keys. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.8:** Author `agents/analyst.md` (NATIVE)
- [x] Read `agent-schemas.md §2.1` (lines 42-77) at `.dev/releases/current/0.1/design/agent-schemas.md` to extract the exact analyst spec — the frontmatter (`name: analyst`, the one-line `description` mentioning source-chapter analysis, pre-orchestration, confidence-tagged facts, ABORTS on NO-ACCESS; `model: opus`; `skills` list = `laf-adaptation:source-fidelity`, `laf-adaptation:adaptation-tiers`, `laf-adaptation:story-memory` (the story-memory entry is the prefix-rewritten form of `creative-writing-skills:story-memory`); `tools: Read, Write, Glob, Grep`), the inputs (`source_path`, `work`, `chapter` — NO `active_tier`, analysis is tier-invariant), the output contract (`work/analysis/ch-<NN>.yaml` per the §3.2 schema), and the Phase-0 ABORT hard-behavior block verbatim (determine SOURCE ACCESS LEVEL ∈ {FULL, PARTIAL, MEMORY-BASED, NO-ACCESS}; IF NO-ACCESS emit `status: ABORTED` and HALT; IF MEMORY-BASED every fact carries confidence: UNCERTAIN), then create the file `analyst.md` at `laf-adaptation/agents/analyst.md` containing the frontmatter (with story-memory in its final `laf-adaptation:story-memory` form) and a body that (a) contains the Phase-0 ABORT hard-behavior block verbatim, (b) instructs loading `/source-fidelity` and executing its 5 phases, (c) declares the inputs `source_path`/`work`/`chapter` and explicitly NOT `active_tier`, (d) declares the output `work/analysis/ch-NN.yaml`, ensuring `model: opus` (never a Mars alias), NO Mars keys in the frontmatter, and the ABORT gate is byte-faithful to the spec. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.9:** Author `agents/safety-verifier.md` (NATIVE)
- [x] Read `agent-schemas.md §2.2` (lines 79-122) at `.dev/releases/current/0.1/design/agent-schemas.md` to extract the exact safety-verifier spec — the frontmatter (`name: safety-verifier`, the one-line `description` mentioning post-generation safety gate for T1-2, 6-section rubric, PASS/FAIL verdict, FAIL blocks kb promotion and re-enters the writer revision loop, Read-only; `model: sonnet`; `skills` = `laf-adaptation:adaptation-safety`, `laf-adaptation:adaptation-tiers`; `tools: Read, Write, Glob, Grep`), the inputs (`draft_path`, `active_tier`, `work`, `chapter` — note it RECEIVES `active_tier` unlike analyst, because it gates on it), the tier-gate block verbatim (IF active_tier ∈ {1,2} run full rubric MANDATORY+blocking; IF ==3 advisory report-only; IF ∈ {4,5} SKIP emit N/A), the output contract (`work/safety-reports/ch-<NN>-t<N>.md` ending in the machine-parseable verdict block from `safety-rubric.md §4`), and the Write-only constraint (Write granted ONLY to emit the report to `work/safety-reports/`; NEVER edits a draft — it is a reviewer not a writer), then read `safety-rubric.md §4` at `.dev/releases/current/0.1/design/safety-rubric.md` to extract the AUTHORITATIVE full verdict-block YAML schema (`verdict` with `work`/`chapter`/`tier`/`result`/`sections`/`automatic_failures`/`mode`/`next`/`evidence` — emit this full §4 form, NOT the abbreviated agent-schemas.md §2.2 snippet), then create the file `safety-verifier.md` at `laf-adaptation/agents/safety-verifier.md` containing the frontmatter and a body that (a) contains the tier-gate block verbatim, (b) declares the inputs, (c) declares the output with the full §4 verdict-block schema, (d) states the Write-only-to-safety-reports / never-edit-draft constraint, ensuring `model: sonnet`, NO Mars keys, and the FAIL→step-3 revision loop wiring is present in the body even though the proof chapter may pass first try (the loop edge must be verifiable per R6 §2). If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.10:** Confirm the `writer.md` ADOPTED-PATCHED graft (verifies Phase-0 Step 2.3 writer item end-to-end)
- [x] Read the vendored `writer.md` at `laf-adaptation/agents/writer.md` and `agent-schemas.md §3` (lines 157-178) at `.dev/releases/current/0.1/design/agent-schemas.md` to confirm the writer graft satisfies Rule C end-to-end, verifying: (a) the `skills:` list contains every prefix-rewritten upstream line PLUS exactly one appended `- laf-adaptation:adaptation-rules` line, (b) the upstream duplicate `creative-writing-craft` line is PRESERVED (not "fixed"), (c) the body (after the closing `---`) is byte-identical to the upstream cw/agents/writer.md (no mode-activation body logic was added — the tier-conditional mode ladder lives in the `/adaptation-rules` SKILL, not the agent), (d) the file is classed ADOPTED-PATCHED in VENDOR.md with two distinct hashes, then use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py` and confirm exit 0 (Rule C passes), ensuring no body byte was changed and the single additive line is the only frontmatter delta. If the graft is malformed (e.g. body edited, duplicate line removed, or non-additive change), fix by re-vendoring from `<checkout>/cw/agents/writer.md` with the correct two transforms and re-run check_boundary.py until exit 0 (max 3 fix cycles). If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.11:** Port `kb/tiers/tier_1.yaml` (verbatim from tier_1_preschool.yaml)
- [x] Read `config/age_profiles/tier_1_preschool.yaml` at `/config/workspace/Infantalizer/config/age_profiles/tier_1_preschool.yaml` to extract its full content verbatim (the T1 profile: `profile`, `developmental_basis`, `thresholds`, `linguistic`, `transformation_rules` with the T1-specific `conflict_to_cooperation`/`death_euphemism` keys and `agency_externalization.mode: mandatory`, plus the T1-only `safety` block with `safe_home_schema` and `nightmare_prevention`), then create the file `tier_1.yaml` at `laf-adaptation/kb/tiers/tier_1.yaml` as a BYTE-FOR-BYTE copy of the source (zero rework — only the filename changes from `tier_1_preschool.yaml` to `tier_1.yaml`), ensuring NO key is renamed (preserve `conflict_to_cooperation`/`death_euphemism` exactly — schema drift is deliberate and absorbed by reader-side key-tolerant lookups, NOT by editing this file), NO value is altered, and the content is byte-identical to the source. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.12:** Port `kb/tiers/tier_2.yaml` (verbatim from tier_2_early_elementary.yaml)
- [x] Read `config/age_profiles/tier_2_early_elementary.yaml` at `/config/workspace/Infantalizer/config/age_profiles/tier_2_early_elementary.yaml` to extract its full content verbatim (T2 profile with the `contest_paradigm` block and `conflict_to_cooperation`/`death_euphemism` keys), then create the file `tier_2.yaml` at `laf-adaptation/kb/tiers/tier_2.yaml` as a BYTE-FOR-BYTE copy of the source (filename `tier_2_early_elementary.yaml` → `tier_2.yaml`), ensuring NO key renamed, NO value altered, byte-identical to source. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.13:** Port `kb/tiers/tier_3.yaml` (verbatim from tier_3_middle_elementary.yaml)
- [x] Read `config/age_profiles/tier_3_middle_elementary.yaml` at `/config/workspace/Infantalizer/config/age_profiles/tier_3_middle_elementary.yaml` to extract its full content verbatim (T3 profile with `tier_transition_summary`, `agency_externalization.mode: optional`, `conflict_to_cooperation`/`death_euphemism` keys), then create the file `tier_3.yaml` at `laf-adaptation/kb/tiers/tier_3.yaml` as a BYTE-FOR-BYTE copy of the source (filename `tier_3_middle_elementary.yaml` → `tier_3.yaml`), ensuring NO key renamed, NO value altered, byte-identical. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.14:** Port `kb/tiers/tier_5.yaml` (verbatim from tier_5_young_adult.yaml)
- [x] Read `config/age_profiles/tier_5_young_adult.yaml` at `/config/workspace/Infantalizer/config/age_profiles/tier_5_young_adult.yaml` to extract its full content verbatim (T5 profile with `adaptation_philosophy` (core_principle "Adaptation means accessibility, not sanitization", when_to_intervene, when_not_to_intervene), `supplementary_approach`, `narrative_complexity.unreliable_narrator: true`/`irony: full_range`, `agency_externalization.mode: forbidden` with reason, and the T5-style `conflict_handling`/`death_handling` keys — NOTE the drift from T1-T3's key names), then create the file `tier_5.yaml` at `laf-adaptation/kb/tiers/tier_5.yaml` as a BYTE-FOR-BYTE copy of the source (filename `tier_5_young_adult.yaml` → `tier_5.yaml`), ensuring the T5 `conflict_handling`/`death_handling` keys are preserved verbatim (do NOT normalize to the T1-T3 names — drift is deliberate), NO value altered, byte-identical. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete. (Do NOT create a tier_4.yaml — T4 is interpolated at runtime, never stored.)

**Step 3.15:** Port `kb/adaptation-mapping/universal-mappings.yaml` (verbatim from universal_mappings.yaml)
- [x] Read `config/concept_mapping/universal_mappings.yaml` at `/config/workspace/Infantalizer/config/concept_mapping/universal_mappings.yaml` to extract its full content verbatim (the 9 concepts: death, evil, war, despair, injury, betrayal, burden, supernatural_terror, martial_heroism — each with `tier_1`/`tier_2`/`tier_3`/`tier_4_5` buckets, non-uniform per-bucket fields, and every `tier_4_5` collapsing to `{translation: "[preserve]"}`), then create the file `universal-mappings.yaml` at `laf-adaptation/kb/adaptation-mapping/universal-mappings.yaml` as a BYTE-FOR-BYTE copy of the source (filename `universal_mappings.yaml` → `universal-mappings.yaml`, `_`→`-`), ensuring all 9 concepts present, no bucket field altered, byte-identical. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.16:** Create native `work/` subdirs (`work/analysis/`, `work/safety-reports/`)
- [x] Read `DESIGN.md §2` (lines 127-131) at `.dev/releases/current/0.1/design/DESIGN.md` to extract the native work subdir contract (`work/analysis/` for per-chapter analyst output, `work/safety-reports/` for safety-verifier output — these are the NATIVE subdirs complementing the adopted `work/drafts/`+`work/critique-reports/` from Phase 0), then create these two native subdirs at `laf-adaptation/work/analysis/` and `laf-adaptation/work/safety-reports/`, ensuring they are empty scaffolds (runtime-populated by the native analyst/safety-verifier agents). If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.17:** Port `templates/work-mapping-template.yaml` (verbatim from work_mapping_template.yaml)
- [x] Read `templates/work_mapping_template.yaml` at `/config/workspace/Infantalizer/templates/work_mapping_template.yaml` to extract its full content verbatim (the onboarding scaffold with the 5 top-level keys `work_metadata`, `characters`, `concepts`, `key_scenes`, `master_translation_table`, `[Bracketed]` fill-ins, two example character slots, the fuller per-character schema with `original`/`tier_1`/`tier_3`/`tier_4_5` blocks), then create the file `work-mapping-template.yaml` at `laf-adaptation/templates/work-mapping-template.yaml` as a BYTE-FOR-BYTE copy of the source (filename `work_mapping_template.yaml` → `work-mapping-template.yaml`, `_`→`-`), ensuring all 5 top-level keys are present (NOT 4 — spec §3.2 prose "four" is a typo; the file + spec enumeration both give 5), no key altered, byte-identical. If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.18:** Update VENDOR.md manifest with NATIVE rows
- [x] Read `boundary-contract.md §2` (lines 74-80) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the NATIVE-row manifest contract (`agents/analyst.md`, `agents/safety-verifier.md` = NATIVE with `—` in both hash columns; `skills/adaptation-tiers/**`, `skills/adaptation-rules/**`, `skills/source-fidelity/**` = NATIVE with `—` in both hash columns — NATIVE files are NOT hash-pinned and MUST NOT collide with an upstream file name per Rule E), then use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py --init --upstream <checkout-dir>` to recompute the manifest now that the native files exist (the script adds the NATIVE rows with `—` hashes and confirms Rule E — no native name collides with upstream), then read the updated `VENDOR.md` at `laf-adaptation/VENDOR.md` to confirm the new rows are present, ensuring analyst/safety-verifier are classed NATIVE, the 3 native skills are listed, NO native file is hash-pinned, and Rule E passes (no collision). If blocked, log in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 3.19:** Phase-1 boundary gate + native-validation verification (L3 test/execute)
- [x] Use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py` (default verify) and capture the output, then write the raw output to `boundary-verify-phase1-output.txt` and a structured summary `boundary-verify-phase1-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/` containing: overall result (PASSED if exit 0), exit code, per-rule pass/fail, and explicit confirmations that (a) `writer.md` diff is frontmatter-only+additive (Rule C), (b) the quartet is intact (Rule D), (c) every native `agents/*.md` + `skills/**/SKILL.md` is now in the manifest (Rule F — analyst, safety-verifier, the 3 native skill SKILL.md files), (d) no native file collides with upstream (Rule E). IF exit 0, proceed. IF exit 1, identify the failing rule/file, fix (e.g. add a missing NATIVE manifest row, undo an accidental adopted-body edit), and re-run (max 3 cycles). If the command fails to execute, log the blocker in ### Phase 3 Findings, then mark this item complete. Once done, mark this item as complete.

### Phase Gate 1: Quality Verification (M3 Lens-Based QA + M4 Source-Document Fidelity Gate)

**Step PG1.1:** Aggregate Phase-1 outputs (L6)
- [x] Use Glob to find all Phase-1 output files matching `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/boundary-verify-phase1-*` and read each, then create the consolidated Phase-1 output summary at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/phase-1-output-summary.md` containing: an executive summary (3 native skills + 2 native agents authored, writer graft verified, 5 kb YAMLs ported, manifest updated with NATIVE rows, boundary gate green), a table of native artifacts with columns Path, Provenance, Source (spec section or port-source file), and an overall Phase-1 verdict (READY FOR PHASE 2 if boundary gate green + native validation passes), ensuring the report accurately aggregates the captured outputs. Once done, mark this item as complete.

**Step PG1.2:** Spawn structural lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa agent with the template-conformance lens to review the 3 native skills + 2 native agents + writer patch against `skill-specs.md §1-§3` and `agent-schemas.md §2-§3`, checking that every native SKILL.md uses ONLY `name`+`description` (NO Mars keys `type`/`model-invocable`/`effort`), every native agent uses the closed Claude-lowered key set (`name`/`description`/`model`/`skills`/`tools` with `model` ∈ {opus,sonnet,haiku,inherit} only), the analyst Phase-0 ABORT block is present verbatim, the safety-verifier tier-gate block is present verbatim, and the writer graft is the single additive `- laf-adaptation:adaptation-rules` line with body byte-identical, with adversarial framing "Assume at least 5 template-conformance errors (a smuggled Mars key, a missing ABORT block, a writer body edit). Find them.", producing `qa-structural-template-conformance-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the internal-consistency lens to review the native spine, checking that the analyst declares NO `active_tier` input while safety-verifier declares it (tier-invariant vs tier-gated), that the story-memory skill reference is in its final `laf-adaptation:story-memory` form (prefix-rewritten) in both agents, that the safety-verifier output contract matches `safety-rubric.md §4` (the full verdict block, not the abbreviated §2.2 snippet), and that the 5 tier commentary files cross-reference the correct tier profiles, with adversarial framing "Assume at least 5 internal inconsistencies (a tier-gate that references the wrong tier, a stale `creative-writing-skills:` prefix, a verdict schema mismatch). Find them.", producing `qa-structural-internal-consistency-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the evidence-quality lens to review the native artifacts, checking that every spec claim cites a `skill-specs.md`/`agent-schemas.md`/`safety-rubric.md` line, that the source-fidelity §3.2 output schema is documented verbatim, that no fabricated examples or assumed behavior appear, and that the writer's preserved duplicate `creative-writing-craft` line is evidenced (not "fixed"), with adversarial framing "Assume at least 5 evidence-quality errors (an uncited claim, a paraphrased schema, a fabricated example). Find them.", producing `qa-structural-evidence-quality-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the completeness lens to review the native spine, checking that all 3 native skills (adaptation-tiers with 5 commentary resources, adaptation-rules with thematic/character/agency resources, source-fidelity) are present, both native agents are present, the writer patch is applied, all 5 ported kb YAMLs (4 tiers + universal-mappings) are present, NO tier_4.yaml exists, and the work-mapping-template is ported, with adversarial framing "Assume at least 5 missing items (a missing commentary file, a missing kb port, a stray tier_4.yaml). Find them.", producing `qa-structural-completeness-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG1.3:** Spawn content lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa-qualitative agent with the actionability lens to review the native skill/agent bodies, checking that every instruction is specific enough to execute (the analyst's "determine SOURCE ACCESS LEVEL" is actionable; the safety-verifier tier-gate has concrete branches; the adaptation-rules "Applying a rule" 4-step procedure is executable), with adversarial framing "Assume at least 5 actionability gaps (a vague instruction, an untestable criterion). Find them.", producing `qa-content-actionability-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the boundary-contract-fidelity lens to run `uv run python laf-adaptation/scripts/check_boundary.py` and confirm exit 0 after the native additions, then independently verify Rule C on writer.md (manual diff confirms frontmatter-only + additive), Rule E (no native file collides with upstream — grep the upstream checkout for analyst.md/safety-verifier.md/adaptation-* skill names and confirm absent), and Rule F (every native agents/*.md + skills/**/SKILL.md is manifest-listed), with adversarial framing "Assume at least 5 boundary-contract breaches (a native file shadowing an upstream name, an unmanifested SKILL.md, a writer body edit). Find them by running the script AND manual checks.", producing `qa-content-boundary-fidelity-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the domain-accuracy lens to verify the native spine matches the design intent, reading `skill-specs.md §1-§3` and `agent-schemas.md §2` and confirming the 4-section adaptation-tiers body, the adaptation-rules cascade reference, the source-fidelity 5-phase protocol + ABORT gate, the analyst tier-invariance, and the safety-verifier distinct-5th-reviewer role (never folded into the quartet), with adversarial framing "Assume at least 5 domain-accuracy errors (a wrong tier paradigm, a folded safety-verifier, a missing ABORT gate). Find them.", producing `qa-content-domain-accuracy-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG1.4:** SOURCE-DOCUMENT FIDELITY GATE (M4/I21 — verbatim carry verification — PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa fidelity agent (fidelity-agent-1) to verify the carried-verbatim kb YAML ports are byte-faithful to their LAF port-source, reading BOTH the port-source files (`config/age_profiles/tier_{1,2,3,5}_*.yaml`, `config/concept_mapping/universal_mappings.yaml`, `templates/work_mapping_template.yaml`) AND the ported targets (`laf-adaptation/kb/tiers/tier_{1,2,3,5}.yaml`, `laf-adaptation/kb/adaptation-mapping/universal-mappings.yaml`, `laf-adaptation/templates/work-mapping-template.yaml`) and confirming each target is a byte-for-byte match to its source (only filename renamed, `_`→`-`), explicitly checking that the schema drift is PRESERVED (T1-T3 `conflict_to_cooperation`/`death_euphemism` vs T5 `conflict_handling`/`death_handling` — do NOT flag as an error, it is deliberate) and that NO key was normalized at port time, with adversarial framing "Assume at least 5 fidelity breaches (a normalized key, an altered value, a dropped field). Find them by direct byte comparison of source vs target.", producing `qa-source-fidelity-report-1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-source-fidelity-report-1.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa fidelity agent (fidelity-agent-2) to verify the carried-verbatim skill-resource YAML wrappers AND the onboarding work-mapping instance are byte-faithful, reading BOTH the port-source (`config/transformation_rules/thematic.yaml`, `config/transformation_rules/character.yaml`, `config/concept_mapping/templates/tolkien_mapping.yaml`) AND the wrapped/copy targets (`laf-adaptation/skills/adaptation-rules/resources/thematic.md`, `.../character.md`, `.../agency.md`, `laf-adaptation/kb/adaptation-mapping/tolkien-mapping.yaml`) and confirming the fenced ```yaml block inside each .md is a byte-for-byte copy of its source YAML (the wrapper prose is the only addition; the YAML payload is unchanged) AND that `tolkien-mapping.yaml` is a byte-for-byte copy of the port-source `tolkien_mapping.yaml` (only filename `_`→`-`), explicitly checking the `death_handling` key in thematic.yaml, the `heroism_translation.subroutine` 3-step block in character.yaml, the `agency_externalization` block carried verbatim from thematic.yaml into agency.md (the agency_externalization YAML payload must be byte-identical; only the ADR-003 rationale prose wrapper is added), and the Tolkien instance's 5 top-level keys + 8 characters + one_ring/mordor/mount_doom/nazgul/army_of_dead concepts are preserved verbatim, with adversarial framing "Assume at least 5 fidelity breaches (a renamed rule group, a paraphrased subroutine, an altered mode enum, a normalized Tolkien concept, an agency_externalization value drift). Find them.", producing `qa-source-fidelity-report-2.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-source-fidelity-report-2.md` with `fix_authorization: false`. (Note: extended per fix-round-1 Q-4 to also cover agency.md and tolkien-mapping.yaml, which were previously unassigned — every carried-verbatim file now has an assigned fidelity verifier: agent-1 covers the 4 tier YAMLs + universal-mappings + work-mapping-template; agent-2 covers thematic + character + agency + tolkien-mapping.) Once done, mark this item as complete.
- [x] Spawn an rf-qa cross-source-contradiction agent to read ALL the port-source files (`config/age_profiles/*`, `config/concept_mapping/*`, `config/transformation_rules/*`, `templates/*`) and check for contradictions BETWEEN them (e.g. does thematic.yaml's `death_handling` key contradict the T1-T3 profiles' `death_euphemism` key? — NO, it is documented drift; does the work-mapping-template have 5 keys while spec §3.2 says 4? — YES, the spec prose is the typo, the file is correct), flagging only genuine contradictions (not documented drift), producing `qa-cross-source-contradictions-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-cross-source-contradictions-report.md` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG1.5:** Consolidate findings and apply fixes (serialized per I20)
- [x] Read all QA reports from Steps PG1.2-PG1.4 (4 structural + 3 content + 3 fidelity = 10 reports in `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/`), consolidate into `qa-fidelity-consolidated-findings.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-fidelity-consolidated-findings.md` listing every issue deduplicated with severity + originating lens/agent, ensuring faithful representation. Once done, mark this item as complete.
- [x] Spawn ONE rf-qa fix agent with `fix_authorization: true` and the consolidated findings as input, applying ALL fixes to the native artifacts (re-port a YAML byte-faithfully, fix a missing ABORT block, undo a key normalization, add a missing manifest row — but NEVER edit an adopted body, and NEVER normalize documented schema drift), producing a fix-applied summary. This is the ONLY agent authorized to modify artifacts in this gate. Once done, mark this item as complete.

**Step PG1.6:** Verification round (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa verification agent to confirm all consolidated findings were addressed and the boundary gate is still green (re-run `uv run python laf-adaptation/scripts/check_boundary.py`, confirm exit 0), producing `qa-fidelity-verification-structural-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative verification agent to confirm content quality + fidelity maintained after fixes (re-verify the byte-faithful carries, the native frontmatter shape), producing `qa-fidelity-verification-content-report-phase1.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Read the two verification reports. IF both PASS, the Phase-1 QA + fidelity gate PASSES — proceed to Phase 2. IF either FAIL, repeat Steps PG1.5-PG1.6 (max 3 cycles per I16 source-fidelity gate); if unresolved after 3 cycles, HALT and escalate, logging in ### Phase Gate Findings. Once done, mark this item as complete.

### Phase 4: Phase 2 — Greenfield (2 Build-New Agents, /adaptation-safety Skill + Genre Resources, kb Adaptation Scaffold)

**Gate:** both build-new agents block on nothing upstream (chronicler writes only on muse-accept; tier-coordinator never writes canon); the native kb adaptation layers are scaffolded; `check_boundary.py` stays green with the BUILD-NEW files added (Rule E — no collision). See `DESIGN.md §7 Phase 2`, `agent-schemas.md §5`, `skill-specs.md §4`, `safety-rubric.md`, `tier-coordinator.md`, `kb-formats.md §4`.

**Step 4.1:** Author `agents/chronicler.md` (BUILD-NEW — graft G1)
- [x] Read `agent-schemas.md §5.1` (lines 213-254) at `.dev/releases/current/0.1/design/agent-schemas.md` and `kb-formats.md §4-§5` (lines 188-281) at `.dev/releases/current/0.1/design/kb-formats.md` to extract the chronicler spec — the frontmatter (`name: chronicler`, the one-line `description` mentioning tier-aware canon extraction, promotion after muse-accept, per-tier continuity keyed (work,tier,chapter); `model: sonnet`; `skills` = `laf-adaptation:story-memory`, `laf-adaptation:kb-management`, `laf-adaptation:adaptation-tiers`; `tools: Read, Write, Glob, Grep` — NO Bash), the inputs (`work`, `chapter`, `active_tier` (the partitioning key), `adapted_path`, `analysis_path`), the DUAL-LAYER write targets (SHARED tier-invariant source truth: `kb/canon/<work>/ch-<NN>.md` APPEND, `kb/timeline/<work>.md`; PER-TIER graft G1: `kb/adaptations/<work>/tier-<N>/continuity.md`, `kb/adaptations/<work>/tier-<N>/chapters/ch-<NN>/canon-delta.md`, `kb/adaptations/<work>/tier-<N>/decisions.md` APPEND), the (work,tier,chapter) keying INVARIANT verbatim ("every per-tier write is keyed (work, tier, chapter). The chronicler MUST NOT write a tier-N fact into another tier's continuity.md, and MUST NOT promote a tier-transformed name into shared kb/canon/"), and the run gate (runs on muse-accept ONLY, per tier, AFTER tier-coordinator returns RECONCILED), then create the file `chronicler.md` at `laf-adaptation/agents/chronicler.md` containing the frontmatter and a body with the THREE invariants authored: (1) key every per-tier write with (work,tier,chapter); (2) NEVER write a tier-N fact into another tier's continuity.md (no cross-tier bleed); (3) NEVER promote a tier-transformed name (e.g. "Grumpy King") into shared kb/canon/ (shared canon = source truth only), ensuring `model: sonnet`, NO Bash tool, NO Mars keys, and the muse-accept-only + post-RECONCILED gate is stated. If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.2:** Author `agents/tier-coordinator.md` (BUILD-NEW — graft G2)
- [x] Read `agent-schemas.md §5.2` (lines 256-284) at `.dev/releases/current/0.1/design/agent-schemas.md` and `tier-coordinator.md §1-§6` (FULL) at `.dev/releases/current/0.1/design/tier-coordinator.md` to extract the tier-coordinator spec — the frontmatter (`name: tier-coordinator`, the one-line `description` mentioning multi-tier fan-out parallel-default/sequential-fallback and reconciliation preserving tier-differentiated framing; `model: opus`; `skills` = `laf-adaptation:adaptation-tiers`, `laf-adaptation:source-fidelity`, `laf-adaptation:kb-management`; `tools: Read, Write, Glob, Grep, Bash` — HAS Bash, unlike chronicler), the inputs/outputs (INPUT: work, chapter, tiers ⊆ {1,2,3,5}, mode ∈ {parallel,sequential} default parallel, shared_analysis = work/analysis/ch-NN.yaml; OUTPUT: work/analysis/ch-NN-cross-tier.md, status: RECONCILED|CONFLICT, conflicts: [] MUST be empty before chronicler runs), the fan-out modes (§2.1 parallel DEFAULT each tier runs steps 3-9 independently reading shared_analysis; §2.2 sequential FALLBACK graft G2 for state-heavy works in ASCENDING order, triggered when `<work>-mapping.yaml.work_metadata.key_challenges` includes `parallel_plotlines`/`unreliable_narrator`/`nested_timeline` OR author sets mode: sequential), the reconcile() THREE checks (§3: A source-fidelity — every tier's transformed element traces to the SAME shared source fact, unsourced ⇒ CONFLICT; B no-downward-disclosure-leak — tier-N disclosures ⊆ permitted for tier-N by its profile; C framing-monotonicity — maturity(tier_N) ≤ maturity(tier_M) whenever N<M), the output report format (§4 verbatim template: shared source anchors, per-tier renderings traceability table, checks A/B/C PASS lines, status: RECONCILED, conflicts: []), the ordering (§5 runs BEFORE chronicler step 10→11, NEVER writes canon, only reconciles+reports), and the T4 handling (§6 interpolated on demand, not persisted), then create the file `tier-coordinator.md` at `laf-adaptation/agents/tier-coordinator.md` containing the frontmatter and a body with the reconcile() 3 checks + fan-out selection heuristic + the report format + the never-writes-canon invariant, ensuring `model: opus`, HAS Bash, NO Mars keys, and the body is faithful to `tier-coordinator.md §3-§5`. If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.3:** Author `skills/adaptation-safety/SKILL.md` (BUILD-NEW — constraint #3)
- [x] Read `skill-specs.md §4` (lines 251-286) at `.dev/releases/current/0.1/design/skill-specs.md` and `safety-rubric.md §1-§4` (FULL) at `.dev/releases/current/0.1/design/safety-rubric.md` to extract the adaptation-safety skill spec — the frontmatter (`name: adaptation-safety`, multi-line `description: |` covering the 6-section post-generation safety rubric for T1-2, forbidden-content scan, agency-externalization, emotional safety, safe-home, nightmare prevention, linguistic compliance, PASS/FAIL verdict, runs on ALL T1-2), and the body carrying the 6 sections + Automatic-Failures list + verdict contract VERBATIM from `prompts/verification/safety_check.md`, then read `prompts/verification/safety_check.md` at `/config/workspace/Infantalizer/prompts/verification/safety_check.md` to source the verbatim 6-section word-lists/checklists (Section 1 forbidden-content scan with T1/T2 forbidden token lists; Section 2 agency-externalization check; Section 3 emotional safety permitted emotions + chapter-ending checklist; Section 4 safe-home schema; Section 5 nightmare prevention; Section 6 linguistic compliance with T1 max-12-words/sentence tier-parameterized from kb/tiers), the automatic-failures list (T1: death language/violence/"evil"/unresolved fear/internal motivation; T2: graphic violence/kill-murder-slay/prolonged hopelessness), the aggregation pseudocode (N/A T4-5 → auto-fail dominates → section-fail → PASS), and the full machine-parseable verdict YAML block from `safety-rubric.md §4`, then create the file `SKILL.md` at `laf-adaptation/skills/adaptation-safety/SKILL.md` containing the frontmatter (Claude-native `name`+`description` ONLY, NO Mars keys, NO `rules/`/`templates/` subdirs) and the body carrying the 6 sections + auto-failures + verdict contract verbatim, ensuring the Section 6 thresholds note they are tier-parameterized from `kb/tiers/tier_<N>.yaml.linguistic` (read at runtime, not hard-coded) and the skill is loaded ONLY by safety-verifier. If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.4:** Author `skills/adaptation-safety/resources/children.md` (genre resource — T1-3 craft)
- [x] Read `skill-specs.md §4.1` (lines 276-278) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the children.md sourcing contract ("prosocial centering, episodic structure, positive resolution, safe-home schema, nightmare-prevention framing, vocabulary/sentence ceilings. Sourced from the tier-1/tier-3 transform prompts and tier profiles"), then read `prompts/transformation/tier_1_transform.md` and `prompts/transformation/tier_3_transform.md` at `/config/workspace/Infantalizer/prompts/transformation/` AND `config/age_profiles/tier_1_preschool.yaml` + `tier_3_middle_elementary.yaml` to extract the concrete craft content (prosocial centering, externalize-negative-agency with the "Sauron was evil"→"Grumpy King" examples, violence→cooperation T1 / weight-not-gore T3, death-handling euphemism→direct-but-gentle, positive/emotional resolution, episodic-structure T1→scaffolded-complexity T3 shift, safe-home schema, nightmare-prevention framing, vocabulary ceilings K→grade 3-5, sentence/chapter ceilings 12-words/800-word T1 → 20-words/2500-word T3, and worked before/after examples), then create the file `children.md` at `laf-adaptation/skills/adaptation-safety/resources/children.md` as AUTHORED craft prose (NOT a verbatim carry — it synthesizes the sources into "how to write children's fiction well across T1-3") organized by the transition arc (T1-2 protect from complexity → T3 scaffold complexity), with explicit cross-references to the rubric's Section 4 (safe-home) + Section 5 (nightmare-prevention) + Section 6 (linguistic), ensuring every dimension is grounded in the cited source files with no fabricated examples, and the doc INFORMS the writer (distinct from the rubric which GATES). If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.5:** Author `skills/adaptation-safety/resources/ya.md` (genre resource — T5 craft)
- [x] Read `skill-specs.md §4.1` (lines 279-282) at `.dev/releases/current/0.1/design/skill-specs.md` to extract the ya.md sourcing contract ("meaning-not-sanitization, unreliable narrator + FULL irony range permitted, when-TO-intervene: archaic language/historical context, when-NOT-to-intervene: difficult themes/moral complexity/character deaths. Sourced from the tier-5 profile's adaptation_philosophy + supplementary_approach"), then read `config/age_profiles/tier_5_young_adult.yaml` at `/config/workspace/Infantalizer/config/age_profiles/tier_5_young_adult.yaml` to extract the verified source keys (`adaptation_philosophy.core_principle` "Adaptation means accessibility, not sanitization", `adaptation_philosophy.when_to_intervene` (archaic language, historical context, obscure cultural references), `adaptation_philosophy.when_not_to_intervene` (difficult themes, moral complexity, character deaths), `supplementary_approach.recommended` (character guide, timeline, context notes, discussion questions), `narrative_complexity.unreliable_narrator: true`/`irony: full_range`, and `agency_externalization.mode: forbidden` reason "Patronizing; undermines character complexity"), then create the file `ya.md` at `laf-adaptation/skills/adaptation-safety/resources/ya.md` as AUTHORED craft prose organized by meaning-not-sanitization, unreliable-narrator+full-irony, when-to-intervene, when-not-to-intervene, and supplementary-approach, ensuring every claim is grounded in the cited tier_5 keys with no fabrication. If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.6:** Scaffold the native kb adaptation layers (`kb/adaptations/`, `kb/adaptation-mapping/` dirs)
- [x] Read `kb-formats.md §4` (lines 188-262) and `DESIGN.md §2` (lines 132-140) at `.dev/releases/current/0.1/design/` to extract the native kb adaptation layer layout (the per-tier canon graft G1 directory shape: `kb/adaptations/<work>/tier-<N>/{decisions.md, continuity.md, chapters/ch-<NN>/{analysis.yaml, adapted.md, canon-delta.md}}` — written by chronicler at runtime on muse-accept; AND the `kb/adaptation-mapping/` dir already scaffolded in Phase 1 with universal-mappings.yaml + the onboarding `<work>-mapping.yaml`), then create the scaffold dirs `laf-adaptation/kb/adaptations/` (empty — runtime-populated by chronicler per work/tier/chapter) and confirm `laf-adaptation/kb/adaptation-mapping/` exists (with universal-mappings.yaml from Phase 1), ensuring these are scaffold-only (the G1 files under `kb/adaptations/<work>/tier-N/` are runtime/proof-time artifacts created during the Phase-3 11-step run, NOT framework-build files) and no content is fabricated. If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.7:** Update VENDOR.md manifest with BUILD-NEW rows
- [x] Read `boundary-contract.md §2` (lines 76-81) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the BUILD-NEW-row manifest contract (`agents/chronicler.md`, `agents/tier-coordinator.md` = BUILD-NEW with `—` in both hash columns; `skills/adaptation-safety/**` = BUILD-NEW with `—` — BUILD-NEW files are NOT hash-pinned and MUST NOT collide with an upstream file name per Rule E; note chronicler is unshipped vapor in BOTH systems so it provably does not collide), then use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py --init --upstream <checkout-dir>` to recompute the manifest with the BUILD-NEW rows, then read the updated `VENDOR.md` to confirm chronicler/tier-coordinator/adaptation-safety are classed BUILD-NEW, NO BUILD-NEW file is hash-pinned, and Rule E passes (no collision — chronicler is absent from upstream, confirming the unshipped-vapor claim). If blocked, log in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 4.8:** Phase-2 boundary gate verification (L3 test/execute)
- [x] Use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py` (default verify) and capture the output, then write the raw output to `boundary-verify-phase2-output.txt` and a structured summary `boundary-verify-phase2-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/` containing: overall result (PASSED if exit 0), exit code, per-rule pass/fail, and confirmations that (a) Rule E holds (chronicler/tier-coordinator/adaptation-safety do NOT shadow upstream names), (b) Rule F holds (the 2 new agents + 1 new skill SKILL.md are manifest-listed), (c) the quartet is still intact (Rule D), (d) writer Rule C still holds. IF exit 0, proceed. IF exit 1, fix and re-run (max 3 cycles). If the command fails to execute, log the blocker in ### Phase 4 Findings, then mark this item complete. Once done, mark this item as complete.

### Phase Gate 2: Quality Verification (M3 Lens-Based QA)

**Step PG2.1:** Aggregate Phase-2 outputs (L6)
- [x] Use Glob to find all Phase-2 output files matching `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/boundary-verify-phase2-*` and read each, then create the consolidated Phase-2 output summary at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/phase-2-output-summary.md` containing: an executive summary (2 build-new agents + adaptation-safety skill + 2 genre resources authored, kb adaptation layers scaffolded, manifest updated with BUILD-NEW rows, boundary gate green), a table of greenfield artifacts with columns Path, Provenance, Source, and an overall Phase-2 verdict (READY FOR PHASE 3 if boundary gate green), ensuring the report accurately aggregates the captured outputs. Once done, mark this item as complete.

**Step PG2.2:** Spawn structural lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa agent with the template-conformance lens to review chronicler.md, tier-coordinator.md, adaptation-safety SKILL.md, children.md, ya.md against `agent-schemas.md §5`, `skill-specs.md §4`, `safety-rubric.md`, `tier-coordinator.md`, checking that chronicler frontmatter has `model: sonnet` + NO Bash, tier-coordinator frontmatter has `model: opus` + HAS Bash, adaptation-safety SKILL.md uses ONLY `name`+`description` (NO Mars keys, NO `rules/`/`templates/` subdirs), and the safety body carries the 6 sections + auto-failures + verdict contract verbatim, with adversarial framing "Assume at least 5 template-conformance errors (a smuggled Mars key, a chronicler with Bash, a missing verdict block). Find them.", producing `qa-structural-template-conformance-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-structural-template-conformance-phase2-report.md` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the internal-consistency lens to review the greenfield artifacts, checking that chronicler's (work,tier,chapter) keying invariant is consistent with tier-coordinator's per-tier fan-out and the kb-formats §4 graft-G1 directory shape, that tier-coordinator's reconcile() 3 checks (A/B/C) match `tier-coordinator.md §3`, that the safety verdict schema matches `safety-rubric.md §4` (full form, not abbreviated), and that the fan-out mode selection heuristic references the correct `key_challenges` markers, with adversarial framing "Assume at least 5 internal inconsistencies (a keying mismatch, a wrong reconcile check, a verdict schema drift). Find them.", producing `qa-structural-internal-consistency-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the evidence-quality lens to review the greenfield artifacts, checking that every spec claim cites an `agent-schemas.md`/`tier-coordinator.md`/`safety-rubric.md`/`skill-specs.md` line, the children.md/ya.md dimensions cite concrete transform-prompt/tier-profile sources, and no fabricated examples appear, with adversarial framing "Assume at least 5 evidence-quality errors (an uncited rubric section, a fabricated ya.md claim, a paraphrased reconcile check). Find them.", producing `qa-structural-evidence-quality-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the completeness lens to review the greenfield artifacts, checking that both build-new agents + adaptation-safety skill + both genre resources + the kb adaptation scaffolds are all present, the reconcile() has all 3 checks, the safety rubric has all 6 sections + auto-failures + aggregation + verdict, and no graft (G1/G2/G3) is silently dropped, with adversarial framing "Assume at least 5 missing items (a dropped reconcile check, a missing genre resource, an absent G3 editor invariant). Find them.", producing `qa-structural-completeness-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG2.3:** Spawn content lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa-qualitative agent with the actionability lens to review the greenfield agent/skill bodies, checking that chronicler's three invariants are concrete and testable, tier-coordinator's reconcile checks produce actionable CONFLICT entries (with type/tier/element), and the safety FAIL→step-3 loop wiring is actionable (verdict.evidence feeds concrete revision targets), with adversarial framing "Assume at least 5 actionability gaps (a vague invariant, an unactionable conflict). Find them.", producing `qa-content-actionability-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the boundary-contract-fidelity lens to run `uv run python laf-adaptation/scripts/check_boundary.py` (confirm exit 0) and verify Rule E explicitly: grep the upstream checkout for chronicler/tier-coordinator/adaptation-safety and confirm they are ABSENT (chronicler is unshipped vapor in both systems; this proves BUILD-NEW provenance), with adversarial framing "Assume at least 5 boundary breaches (a BUILD-NEW file shadowing upstream, an unmanifested file). Find them.", producing `qa-content-boundary-fidelity-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the domain-accuracy lens to verify the greenfield artifacts match the design intent, reading `agent-schemas.md §5` + `tier-coordinator.md` + `safety-rubric.md` and confirming chronicler's muse-accept-only gate, tier-coordinator's never-writes-canon invariant + runs-before-chronicler ordering, the safety-verifier distinct-5th-reviewer role, and the genre resources inform-not-gate distinction, with adversarial framing "Assume at least 5 domain-accuracy errors (a chronicler that writes canon pre-accept, a tier-coordinator that writes canon, a genre resource that gates). Find them.", producing `qa-content-domain-accuracy-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG2.4:** Consolidate findings and apply fixes (serialized per I20)
- [x] Read all 7 QA reports from Steps PG2.2-PG2.3, consolidate into `qa-consolidated-findings-phase2.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-consolidated-findings-phase2.md` listing every issue deduplicated with severity + originating lens. Once done, mark this item as complete.
- [x] Spawn ONE rf-qa fix agent with `fix_authorization: true` and the consolidated findings as input, applying ALL fixes to the greenfield artifacts (NEVER editing adopted files — only chronicler/tier-coordinator/adaptation-safety/children.md/ya.md), producing a fix-applied summary. Once done, mark this item as complete.

**Step PG2.5:** Verification round (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa verification agent to confirm all findings addressed and the boundary gate still green (re-run check_boundary.py, exit 0), producing `qa-verification-structural-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative verification agent to confirm content quality maintained, producing `qa-verification-content-phase2-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Read the two verification reports. IF both PASS, the Phase-2 QA gate PASSES — proceed to Phase 3 (the HARD GATE). IF either FAIL, repeat Steps PG2.4-PG2.5 (max 3 cycles); if unresolved after 3 cycles, HALT and escalate, logging in ### Phase Gate Findings. Once done, mark this item as complete.

### Phase 5: Phase 3 — Compose & Prove (HARD GATE — the integration test, definition of "0.1 done")

**Gate (5 conditions, `DESIGN.md §7 line 296`, all must be GREEN or HALT):** (1) v2.0 CERTAIN/PROBABLE/UNCERTAIN tags present in analyst output; (2) safety PASS; (3) per-tier canon written (continuity.md + canon-delta.md per tier); (4) all four quartet agents ran (critic/editor/reader-sim/continuity-checker); (5) `check_boundary.py` green. Deliverable: full 11-step workflow on ONE Tolkien chapter at Tier 1, then the same chapter at Tiers 1/3/5 via `tier-coordinator`. See `DESIGN.md §3 + §7 Phase 3`, `agent-schemas.md §6`, `tier-coordinator.md`, `safety-rubric.md §4.1`.

**Step 5.1:** Provision the proof chapter source (source/ scaffold + chapter file)
- [x] Read `DESIGN.md §2` (line 126) and `§6` (line 282) and `§7 Phase 3` (line 296) at `.dev/releases/current/0.1/design/DESIGN.md` to extract the proof-chapter contract (`source/<work>/ch-<NN>.txt` is a read-only reference; the proof runs on "one Tolkien chapter"; the analyst's Phase-0 ABORT gate treats source as an external/provisioned input), then create the `source/` scaffold at `laf-adaptation/source/<work>/` (using `<work>` = a Tolkien work label, e.g. `lotr` or `hobbit`) and a `source/README.md` documenting the expected `ch-NN.txt` contract (UTF-8, one chapter per file, the analyst reads it as `source_path`), then provision the actual proof chapter `source/<work>/ch-01.txt` by ONE of two paths: (PATH A — PREFERRED, execution-time external input) place a short single chapter of the actual work under adaptation, supplied by the operator under their own access rights (the operator supplies the prose at execution time — DO NOT commit copyrighted Tolkien prose), OR (PATH B — committable fallback) place a short PUBLIC-DOMAIN narrative excerpt (e.g. a Grimm/Andersen fairy-tale chapter, or a Tolkien-adjacent PD source like the Kalevala/Norse Eddas/Beowulf) purely to prove the pipeline mechanics, ensuring NO real Tolkien prose (still under copyright in most jurisdictions — US expiry well after 2026) is committed as a fixture, and the `source_path` input to the analyst resolves to this file. If neither path is available (no operator-supplied chapter AND no PD stand-in chosen), log this as a blocker in ### Phase 5 Findings AND document the assumption made (e.g. proceeded with a PD stand-in labeled as such), then mark this item complete. Once done, mark this item as complete.

**Step 5.2:** Instantiate the work-mapping for the proof work
- [x] Read `kb-formats.md §3.2-§3.3` (lines 144-184) at `.dev/releases/current/0.1/design/kb-formats.md` to extract the `<work>-mapping.yaml` onboarding contract (created per work by copying `templates/work-mapping-template.yaml`; the Tolkien instance is pre-populated), then read `templates/work-mapping-template.yaml` at `laf-adaptation/templates/work-mapping-template.yaml` AND the pre-populated Tolkien instance at `/config/workspace/Infantalizer/config/concept_mapping/templates/tolkien_mapping.yaml`, then create the instantiated work-mapping `tolkien-mapping.yaml` (or `<work>-mapping.yaml` matching the `<work>` label from Step 5.1) at `laf-adaptation/kb/adaptation-mapping/<work>-mapping.yaml` by copying the Tolkien instance verbatim (it has the 5 top-level keys work_metadata/characters/concepts/key_scenes/master_translation_table; 8 characters; the one_ring/mordor/mount_doom/nazgul/army_of_dead concepts; the battle_pelennor/mount_doom_climax/grey_havens key_scenes), ensuring all 5 top-level keys present (NOT 4), no content fabricated beyond the Tolkien instance, and the cascade can resolve tier translations from this file. If the proof work is not Tolkien, copy the template and fill in the `[Bracketed]` placeholders for the chosen work instead. If blocked, log in ### Phase 5 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 5.3:** Run the full 11-step workflow at Tier 1 (L3 test/execute — the integration test core)
- [x] Read `DESIGN.md §3` (lines 158-209) at `.dev/releases/current/0.1/design/DESIGN.md` to extract the 11-step per-chapter workflow (step 1 analyst NATIVE → ABORT gate → step 2 muse ADOPTED → step 3 writer ADOPTED+adaptation-rules → step 4 critic×N ADOPTED → step 5 editor ADOPTED never-folded → step 6 writer revision → step 7 continuity-checker ADOPTED → step 8 safety-verifier NATIVE → FAIL-branch back to step 3 → step 9 reader-sim ADOPTED → step 10 tier-coordinator BUILD-NEW → step 11 chronicler BUILD-NEW on muse-accept), then execute the full 11-step pipeline on the proof chapter at `active_tier=1`, dispatching each agent in sequence with the correct inputs (analyst gets `source_path`/`work`/`chapter` NO `active_tier`; muse gets analysis + tier_1 profile + mapping; writer gets brief + adaptation-rules + tier_1; critic/editor/continuity-checker get the draft; safety-verifier gets draft + `active_tier=1`; reader-sim gets draft + the tier-1 developmental_basis persona; tier-coordinator gets the tier-1 adapted output; chronicler runs on muse-accept keyed (work,1,chapter)), capturing every step's output to its canonical path (`work/analysis/ch-01.yaml`, `work/drafts/ch-01-t1-v*.md`, `work/critique-reports/`, `work/safety-reports/ch-01-t1.md`, `kb/adaptations/<work>/tier-1/continuity.md` + `chapters/ch-01/canon-delta.md` + `decisions.md`), then write the raw per-step capture to `phase3-t1-run-raw.txt` and a structured summary `phase3-t1-run-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/` containing: per-step status (ran/skipped/FAILED), the agent provenance per step, the artifact paths produced, the safety verdict (PASS/FAIL/N/A), whether the FAIL→step-3 loop was exercised (it is OK if the chapter passes first try, but the loop WIRING in safety-verifier must be present — note this), and the overall T1 run result (CONVERGED if all steps ran and safety PASS), ensuring the summary faithfully records what actually ran (no fabricated steps). If a step FAILS to execute (e.g. an agent produces no output), log the specific failure in ### Phase 5 Findings AND attempt a fix (re-dispatch with corrected inputs), max 3 retries per step. Once done, mark this item as complete.

**Step 5.4:** Run the multi-tier fan-out at T1/3/5 via tier-coordinator (L3 test/execute)
- [x] Read `DESIGN.md §3.2` (lines 226-238) at `.dev/releases/current/0.1/design/DESIGN.md` and `tier-coordinator.md §2-§4` at `.dev/releases/current/0.1/design/tier-coordinator.md` to extract the fan-out contract (analyst runs ONCE per chapter — tier-invariant source truth shared across tiers; tier-coordinator fans the transform across T1/T3/T5 — each tier runs steps 3-9 independently reading the same shared_analysis; parallel DEFAULT, sequential G2 fallback for state-heavy works; chronicler runs per tier after RECONCILED), then execute the multi-tier fan-out: confirm the analyst's `work/analysis/ch-01.yaml` (from Step 5.3) is the shared source truth, dispatch the steps-3-9 pipeline at `active_tier=3` and `active_tier=5` (T1 already done in Step 5.3), then run `tier-coordinator` with `tiers=[1,3,5]`, `mode=parallel` (default), reading the shared_analysis, producing the cross-tier report `work/analysis/ch-01-cross-tier.md` with the per-tier renderings traceability table + the 3 reconcile checks (A source-fidelity PASS, B disclosure-leak PASS, C monotonicity PASS) + `status: RECONCILED` + `conflicts: []`, then run `chronicler` per tier (T1, T3, T5) keyed (work,tier,ch-01) on muse-accept, writing `kb/adaptations/<work>/tier-{1,3,5}/{continuity.md, chapters/ch-01/canon-delta.md, decisions.md}`, then write the raw capture to `phase3-fanout-raw.txt` and a structured summary `phase3-fanout-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/` containing: per-tier convergence, the tier-coordinator status (RECONCILED/CONFLICT), the conflicts list (MUST be empty for chronicler to run), the per-tier chronicler artifact paths, and confirmation that NO tier-transformed name was promoted to shared `kb/canon/` (e.g. "Grumpy King" stays in tier-1 continuity only). IF status=CONFLICT (non-empty conflicts), log the conflicts in ### Phase 5 Findings, re-dispatch the offending tier's writer with the conflict as a revision note, and re-run tier-coordinator (max 3 cycles — sequential G2 fallback may be tried if parallel keeps conflicting for a state-heavy work). Once done, mark this item as complete.

**Step 5.5:** Verify runtime artifacts produced (L4 review of the runtime output set)
- [x] Read `kb-formats.md §4` (lines 188-262) at `.dev/releases/current/0.1/design/kb-formats.md` to extract the runtime-artifact contract (the G1 files: continuity.md with its 4 sections + I-1/I-2/I-3 invariants; decisions.md append log; canon-delta.md 3-section per-chapter delta; chapters/ch-NN/analysis.yaml promoted copy + adapted.md; plus shared kb/canon/<work>/ch-NN.md + kb/timeline/<work>.md), then use Glob to discover all runtime artifacts produced by Steps 5.3-5.4 under `laf-adaptation/work/` and `laf-adaptation/kb/adaptations/<work>/` and `laf-adaptation/kb/canon/<work>/` and `laf-adaptation/kb/timeline/`, then read each to verify it conforms to its format (continuity.md cites source characters/facts I-1, contains no fact a Tier-N reader could not know I-2, holds tier-transformed names only I-3; analysis.yaml carries the v2.0 confidence tags; the chronicler never promoted a transformed name to shared canon), then create the review file `phase3-runtime-artifacts-review.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/phase3-runtime-artifacts-review.md` containing: overall verdict (PASS/FAIL), a table of expected vs actual artifacts, the I-1/I-2/I-3 invariant checks per tier, and any missing/malformed artifacts flagged, ensuring the review is based entirely on the actual produced files with no assumed presence. If artifacts are missing or malformed, log in ### Phase 5 Findings (they may need a re-run of the relevant step), then mark this item complete. Once done, mark this item as complete.

**Step 5.6:** HARD-GATE condition 1 — v2.0 tags present (L4 verdict)
- [x] Read the analyst output `work/analysis/ch-01.yaml` at `laf-adaptation/work/analysis/ch-01.yaml` and `skill-specs.md §3.2` at `.dev/releases/current/0.1/design/skill-specs.md` to verify the v2.0 Pragmatic Verification Protocol tags, then create the verdict file `gate-cond1-tags.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/gate-cond1-tags.md` containing: overall verdict (PASS if the analysis.yaml carries CERTAIN/PROBABLE/UNCERTAIN confidence tags on essentials/characters/events/summary AND a valid `source_access` declaration AND `status: OK` (not ABORTED) — proving the analyst ran the v2.0 protocol; FAIL if tags are absent or status is ABORTED), the specific tags found, and any gaps, ensuring the verdict is grounded in the actual file content. If the tags are absent, log in ### Phase 5 Findings (the analyst agent may need re-dispatch), then mark this item complete. Once done, mark this item as complete.

**Step 5.7:** HARD-GATE condition 2 — safety PASS (L4 verdict)
- [x] Read the safety report `work/safety-reports/ch-01-t1.md` at `laf-adaptation/work/safety-reports/ch-01-t1.md` and `safety-rubric.md §4` at `.dev/releases/current/0.1/design/safety-rubric.md` to verify the safety verdict, then create the verdict file `gate-cond2-safety.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/gate-cond2-safety.md` containing: overall verdict (PASS if the machine-parseable `verdict:` block shows `result: PASS` for tier 1 with `mode: blocking` and `next: promote` — proving the safety-verifier ran the full 6-section rubric and the chapter passed; FAIL if `result: FAIL` — which would mean the FAIL→step-3 revision loop should have fired, so also check whether the loop was exercised), the section-by-section results, the automatic_failures list (must be empty for PASS), and confirmation the tier-gate ran in blocking mode for T1. If result=FAIL and the loop did NOT fire (a wiring bug), log in ### Phase 5 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 5.8:** HARD-GATE condition 3 — per-tier canon written (L4 verdict)
- [x] Read the per-tier canon files `kb/adaptations/<work>/tier-{1,3,5}/continuity.md` and `.../chapters/ch-01/canon-delta.md` at `laf-adaptation/kb/adaptations/<work>/` and `kb-formats.md §4.1-§4.3` to verify the per-tier canon, then create the verdict file `gate-cond3-canon.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/gate-cond3-canon.md` containing: overall verdict (PASS if continuity.md + canon-delta.md exist for EACH tier run (T1, T3, T5) AND the chronicler keying invariant holds — every per-tier write keyed (work,tier,ch-01) AND no tier-transformed name promoted to shared kb/canon/ AND no cross-tier bleed; FAIL if any tier's canon is missing or the keying invariant is violated), the per-tier artifact list, the transformed-name check (e.g. confirm "Grumpy King" appears ONLY in tier-1 continuity, NOT in shared kb/canon/<work>/ch-01.md), and the (work,tier,chapter) key verification. If any tier's canon is missing or the invariant is violated, log in ### Phase 5 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 5.9:** HARD-GATE condition 4 — all four quartet agents ran (L4 verdict)
- [x] Read the Phase-3 run summaries `phase3-t1-run-summary.md` and `phase3-fanout-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/` and `DESIGN.md §3` (lines 182-187, the quartet) to verify the quartet, then create the verdict file `gate-cond4-quartet.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/gate-cond4-quartet.md` containing: overall verdict (PASS if all FOUR distinct quartet agents — critic (step 4), editor (step 5), continuity-checker (step 7), reader-sim (step 9) — each executed during the T1 run AND the editor was NEVER folded (G3 — it ran as a distinct agent, not merged into critic or writer) AND the safety-verifier ran as the distinct 5th reviewer (step 8); FAIL if any quartet agent was skipped, folded, or merged), the per-quartet-agent ran-confirmation with its output path, and the editor-never-folded assertion. If any quartet agent was skipped/folded, log in ### Phase 5 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 5.10:** HARD-GATE condition 5 — check_boundary.py green (L4 verdict)
- [x] Use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py` (default verify) and capture the output, then create the verdict file `gate-cond5-boundary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/gate-cond5-boundary.md` containing: overall verdict (PASS if exit 0 — proving every ADOPTED file is still byte-identical to its manifest hash after the full 11-step + fan-out run, the writer graft is still frontmatter-only+additive, the quartet is intact, and no native/build-new file collides with upstream; FAIL if exit 1 — meaning an adopted body was accidentally edited during the proof run, a regression), the exit code, the per-rule pass/fail, and the raw output tail. The boundary contract MUST stay green THROUGH the proof run (this is constraint #6 enforced at runtime) — a FAIL here means an adopted file drifted during execution, which is exactly what the contract prevents; if FAIL, log the offending file/rule in ### Phase 5 Findings, restore the adopted file from upstream with the correct transform, and re-run (max 3 cycles). Once done, mark this item as complete.

**Step 5.11:** Aggregate the 5 gate conditions into the Phase-3 HARD-GATE report (L6 — GREEN only if all 5 PASS, else HALT)
- [x] Use Glob to find the 5 gate-condition verdict files matching `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/gate-cond*-*.md`, then read each to extract its verdict (PASS/FAIL) and key evidence, then create the consolidated Phase-3 HARD-GATE report at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/phase-3-hard-gate-report.md` containing: an executive summary with a 5-row table (Condition 1 v2.0 tags / Condition 2 safety PASS / Condition 3 per-tier canon / Condition 4 quartet ran / Condition 5 boundary green — each with its verdict + evidence pointer), the OVERALL gate verdict (GREEN only if ALL 5 = PASS; RED if ANY = FAIL), the proof-chapter provisioning path used (external input vs PD stand-in), the tier-coordinator status (RECONCILED + empty conflicts), and a final statement ("0.1 is DONE" if GREEN, or "0.1 is BLOCKED — gate condition(s) N failed: <details>" if RED), ensuring the report accurately aggregates the 5 verdict files with no fabricated results and NO condition is marked PASS without a corresponding green verdict file. IF the overall verdict is RED, this is a HARD HALT — "0.1 is NOT done"; log the failing condition(s) in ### Phase 5 Findings, address the root cause (re-run the failing step / fix the offending artifact), and re-evaluate the affected condition(s) (max 3 gate-retry cycles); if still RED after 3 cycles, HALT and escalate to the user — the 0.1 release cannot ship. Once done, mark this item as complete.

### Phase Gate 3: Quality Verification (M3 Lens-Based QA on the HARD-GATE report + runtime artifacts)

**Step PG3.1:** Aggregate Phase-3 outputs (L6)
- [x] Use Glob to find all Phase-3 output files matching `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/{test-results,reviews,reports}/phase3-*` and `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/phase-3-hard-gate-report.md`, read each, then create the consolidated Phase-3 QA-input summary at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/phase-3-output-summary.md` containing: an executive summary (full 11-step T1 run + T1/3/5 fan-out executed, 5 gate conditions evaluated, overall gate verdict), a table of runtime artifacts, and the gate-condition rollup, ensuring accurate aggregation. Once done, mark this item as complete.

**Step PG3.2:** Spawn structural lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa agent with the template-conformance lens to review the Phase-3 runtime artifacts against `kb-formats.md §4` and `skill-specs.md §3.2`, checking that continuity.md has its 4 sections + the I-1/I-2/I-3 invariants, canon-delta.md has its 3 sections, analysis.yaml conforms to the §3.2 schema, the safety report ends in the §4 machine-parseable verdict block, and the cross-tier report has the §4 tier-coordinator format, with adversarial framing "Assume at least 5 template-conformance errors (a malformed verdict block, a missing continuity section, a schema-violating analysis.yaml). Find them.", producing `qa-structural-template-conformance-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the internal-consistency lens to review the Phase-3 artifacts, checking that the cross-tier report's per-tier renderings trace to the shared analysis anchors, the chronicler's per-tier writes are consistently keyed (work,tier,chapter), the safety verdict's section rollups agree with the overall result, and the gate-condition verdicts in the hard-gate report match the individual gate-cond files, with adversarial framing "Assume at least 5 internal inconsistencies (a rendering that doesn't trace, a keying mismatch, a verdict tallied wrong). Find them.", producing `qa-structural-internal-consistency-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the evidence-quality lens to review the hard-gate report, checking that every PASS verdict cites a concrete artifact path that EXISTS, no verdict is assumed without evidence, and the run summaries faithfully record what ran (no fabricated steps), with adversarial framing "Assume at least 5 evidence-quality errors (a PASS without an artifact, an assumed run, a missing citation). Find them.", producing `qa-structural-evidence-quality-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the completeness lens to review the Phase-3 run, checking that all 11 steps ran at T1, all 3 tiers (T1/T3/T5) ran in the fan-out, all 5 gate conditions were evaluated, and all expected runtime artifacts per tier exist, with adversarial framing "Assume at least 5 missing items (a skipped step, a missing tier, an untested condition). Find them.", producing `qa-structural-completeness-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG3.3:** Spawn content lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa-qualitative agent with the actionability lens to review the Phase-3 run, checking that the cross-tier report's conflicts (if any) are actionable, the safety evidence pinpoints located revision targets, and the gate report's RED path (if any) names the specific failing condition with a remediation pointer, with adversarial framing "Assume at least 5 actionability gaps (an unactionable conflict, vague safety evidence). Find them.", producing `qa-content-actionability-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the boundary-contract-fidelity lens to run `uv run python laf-adaptation/scripts/check_boundary.py` and confirm exit 0 (proving the proof run did NOT drift any adopted file), AND independently verify the chronicler invariant (grep shared `kb/canon/<work>/ch-01.md` for tier-transformed names like "Grumpy King" and confirm ABSENT — they belong only in tier-1 continuity), with adversarial framing "Assume at least 5 boundary/canon breaches (an adopted body drift, a transformed name leaked to shared canon, a cross-tier bleed). Find them.", producing `qa-content-boundary-fidelity-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the domain-accuracy lens to verify the Phase-3 outputs match the design intent, reading `DESIGN.md §3` and confirming the 11-step provenance tally (NATIVE analyst+safety-verifier, ADOPTED quartet+muse+writer, BUILD-NEW tier-coordinator+chronicler), the tier-differentiated framing (T1 "Grumpy King" vs T5 near-source), and the parallel-fan-out topology, with adversarial framing "Assume at least 5 domain-accuracy errors (a wrong provenance, an undifferentiated tier rendering, a folded agent). Find them.", producing `qa-content-domain-accuracy-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PG3.4:** Consolidate findings and apply fixes (serialized per I20)
- [x] Read all 7 QA reports from Steps PG3.2-PG3.3, consolidate into `qa-consolidated-findings-phase3.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-consolidated-findings-phase3.md` listing every issue deduplicated with severity + originating lens. Once done, mark this item as complete.
- [x] Spawn ONE rf-qa fix agent with `fix_authorization: true` and the consolidated findings as input, applying ALL fixes to the Phase-3 runtime artifacts + hard-gate report (re-running a step if needed, correcting a malformed artifact, NEVER editing an adopted file), producing a fix-applied summary. Once done, mark this item as complete.

**Step PG3.5:** Verification round (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa verification agent to confirm all findings addressed, the hard-gate report is still GREEN (all 5 conditions), and check_boundary.py is still exit 0, producing `qa-verification-structural-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative verification agent to confirm runtime-artifact quality + invariant maintenance after fixes, producing `qa-verification-content-phase3-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Read the two verification reports. IF both PASS AND the Phase-3 hard-gate report is GREEN, the Phase-3 QA gate PASSES — proceed to Phase 4. IF either FAIL OR the hard gate is RED, repeat Steps PG3.4-PG3.5 (max 3 cycles); if the hard gate remains RED after 3 cycles, HALT and escalate — 0.1 is NOT done. Once done, mark this item as complete.

### Phase 6: Phase 4 — Upstream-Sync Protocol (Documentation Deliverable)

**Gate:** the upstream-sync protocol is documented; NATIVE/BUILD-NEW files are provably never touched by the sync flow; the SHA re-pin procedure is specified. See `DESIGN.md §7 Phase 4`, `boundary-contract.md §5`.

**Step 6.1:** Document the 6-step upstream-sync protocol
- [x] Read `boundary-contract.md §5` (lines 185-202) at `.dev/releases/current/0.1/design/boundary-contract.md` to extract the verbatim 6-step upstream-sync protocol (1. Fetch upstream; pick new target SHA. 2. For each ADOPTED-CLEAN file: 3-way reconcile base=upstream@old_sha (prefix-rewritten), ours=laf file (==base by contract), theirs=upstream@new_sha (prefix-rewritten); since ours==base apply theirs cleanly fast-forward, conflicts⇒manual cherry-pick. 3. For writer.md ADOPTED-PATCHED: reapply the single additive skill line on top of theirs' body. 4. NEVER touch NATIVE/BUILD-NEW files during sync. 5. Re-run check_boundary.py --init to recompute hashes; update upstream_sha + vendored_on in VENDOR.md. 6. Re-run the Phase-3 hard gate before shipping) AND the payoff rationale ("Because adopted files are provably unmodified (ours==base), upstream patches apply without merge pain — that is the entire payoff of the boundary contract. The moment an adopted file drifts, this fast-forward degrades to manual cherry-pick."), then create the documentation file `UPSTREAM-SYNC.md` at `laf-adaptation/UPSTREAM-SYNC.md` containing: the 6-step procedure verbatim (with the 3-way-merge explanation for ADOPTED-CLEAN and the writer re-graft note for ADOPTED-PATCHED), an explicit "NEVER touch NATIVE/BUILD-NEW files" callout (constraint — these are LAF-authored and outside the upstream relationship), the Phase-4 re-init command (`uv run python scripts/check_boundary.py --init --upstream <new-checkout-dir>`), the Phase-3 hard-gate re-run requirement (all 5 conditions must re-pass before the synced version ships), and the payoff rationale, ensuring the doc is faithful to `boundary-contract.md §5` with no fabricated steps. If blocked, log in ### Phase 6 Findings, then mark this item complete. Once done, mark this item as complete.

**Step 6.2:** Verify NATIVE/BUILD-NEW files are never touched by the sync flow (L4 review)
- [x] Read the authored `UPSTREAM-SYNC.md` at `laf-adaptation/UPSTREAM-SYNC.md` and `boundary-contract.md §5` (lines 185-202) to verify the sync-flow scoping, then create the review file `phase4-sync-scope-review.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reviews/phase4-sync-scope-review.md` containing: overall verdict (PASS if the documented sync flow touches ONLY ADOPTED-CLEAN + ADOPTED-PATCHED files — step 2 and 3 respectively — and step 4 explicitly excludes NATIVE/BUILD-NEW; FAIL if any step could touch a native/build-new file), the per-step scope (which provenance class each step touches), and the Phase-3-hard-gate re-run requirement confirmation. If the doc could touch native files, log in ### Phase 6 Findings and fix the doc, then mark this item complete. Once done, mark this item as complete.

**Step 6.3:** Final boundary gate confirmation (L3 test/execute — the tree is complete)
- [x] Use the Bash tool to run `uv run python laf-adaptation/scripts/check_boundary.py` (default verify) on the now-complete `laf-adaptation/` tree (all 15 agents, 16 skills, kb layers, scripts, root files present) and capture the output, then write the raw output to `boundary-verify-final-output.txt` and a structured summary `boundary-verify-final-summary.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/test-results/` containing: overall result (PASSED if exit 0), exit code, per-rule pass/fail (A-F all green), the total manifest row count (must cover all 15 agents + 16 skill SKILL.md + all adopted resources/**), and a statement that the boundary contract holds on the COMPLETE tree. IF exit 0, the tree is boundary-clean — proceed. IF exit 1, log the failing rule/file in ### Phase 6 Findings, fix, and re-run (max 3 cycles). Once done, mark this item as complete.

## Post-Completion Actions

- [x] Verify all task outputs by using Glob to confirm every output file specified in checklist items exists on disk — specifically the 15 `laf-adaptation/agents/*.md`, the 16 `laf-adaptation/skills/*/SKILL.md` (+ native/build-new resources: tier_1-5.md, thematic/character/agency.md, children/ya.md), the 5 ported `laf-adaptation/kb/tiers/*.yaml` + `kb/adaptation-mapping/universal-mappings.yaml`, `laf-adaptation/templates/work-mapping-template.yaml`, `laf-adaptation/scripts/check_boundary.py`, `laf-adaptation/VENDOR.md`, `laf-adaptation/NOTICE`, `laf-adaptation/LICENSE-CWS`, `laf-adaptation/CLAUDE.md`, `laf-adaptation/UPSTREAM-SYNC.md`, `laf-adaptation/.githooks/pre-commit`, `.github/workflows/boundary.yml`, the Phase-3 runtime artifacts (`work/analysis/*.yaml`, `work/drafts/*`, `work/safety-reports/*`, `kb/adaptations/<work>/tier-{1,3,5}/*`), and the phase-outputs handoff files — ensuring no expected deliverables are missing. If any files are missing, check the Task Log for blockers explaining the absence; if files are missing without documented reason, log the gap in ### Follow-Up Items below, then mark this item complete.

- [x] This task authored exactly ONE Python script (`scripts/check_boundary.py`, the validation tool — the framework itself is prompt+YAML config with no unit-test suite per ADR-006). NOTE: check_boundary.py verify/init/report all functional — boundary contract green (verify exit 0; --report exit 0 printing the 64-row provenance summary 55 ADOPTED-CLEAN + 1 ADOPTED-PATCHED + 5 NATIVE + 3 BUILD-NEW; --init exercised in Phases 0-2). Run the final boundary-script verification by using the Bash tool to execute `uv run python laf-adaptation/scripts/check_boundary.py` and confirm exit 0 (this re-confirms the Phase-6 Step 6.3 green state is stable; the script exercises all 6 verify-algorithm rules A-F). Additionally confirm the `--init` and `--report` modes are functional (the `--init` was exercised in Phases 0-2; run `uv run python laf-adaptation/scripts/check_boundary.py --report` once and confirm it exits 0 and prints a human-readable provenance summary). If any mode errors, log the specific failure in ### Phase 6 Findings. If all modes pass, note "check_boundary.py verify/init/report all functional — boundary contract green" in the Task Log and mark this item complete.

**POST-COMPLETION LENS-BASED QA — FINAL ASSEMBLED-OUTPUT GATE (MANDATORY per I17 items 5-6)**

This final gate verifies the COMPLETE `laf-adaptation/` assembled tree (all phases integrated), catching cross-phase interactions. Minimum 6 agents (3 rf-qa structural + 3 rf-qa-qualitative content) per I19 full intensity.

**Step PC.1:** Aggregate the complete assembled tree (L6)
- [x] Use Glob to enumerate the complete `laf-adaptation/` tree (`laf-adaptation/agents/*.md`, `laf-adaptation/skills/**`, `laf-adaptation/kb/**`, `laf-adaptation/work/**`, `laf-adaptation/templates/**`, `laf-adaptation/scripts/*.py`, `laf-adaptation/*.md` root files, `laf-adaptation/.githooks/*`) and read the phase-output summaries (`phase-0-output-summary.md`, `phase-1-output-summary.md`, `phase-2-output-summary.md`, `phase-3-output-summary.md`, `phase-3-hard-gate-report.md`, `boundary-verify-final-summary.md`), then create the final assembled-tree manifest at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/phase-outputs/reports/final-assembled-tree-summary.md` containing: a complete file inventory grouped by provenance class (ADOPTED-CLEAN / ADOPTED-PATCHED / NATIVE / BUILD-NEW) with counts (must reconcile to 15 agents + 16 skills + 5 kb YAMLs + 1 template + 1 script + root files), the per-phase gate verdict rollup (Phase 0/1/2/3 all PASS), the Phase-3 hard-gate verdict (GREEN), and the final boundary-gate verdict (exit 0), ensuring the manifest accurately reflects the actual tree. Once done, mark this item as complete.

**Step PC.2:** Spawn structural lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa agent with the template-conformance lens to review the COMPLETE assembled tree, checking that every adopted file uses the cw-lowered dialect (NO Mars keys anywhere), every native/build-new file uses Claude-native frontmatter, VENDOR.md has all 5 header fields + 3-line Invariants + complete manifest, NOTICE + LICENSE-CWS + CLAUDE.md + UPSTREAM-SYNC.md are all present and well-formed, and no placeholder/sentinel text remains anywhere, with adversarial framing "Assume the assembled tree has at least 10 template-conformance errors across all phases (a smuggled Mars key, a malformed manifest, a placeholder hash, a missing root file). Find them.", producing `qa-post-completion-template-conformance-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the internal-consistency lens to review the COMPLETE tree cross-phase, checking that the VENDOR.md manifest row count reconciles to the actual agents/*.md + skills/**/SKILL.md count (Rule F), the ported kb YAMLs' schema drift is consistently preserved (T1-T3 conflict_to_cooperation vs T5 conflict_handling) and consistently consumed by key-tolerant lookups in the skills, the safety-verifier verdict schema matches across the agent body + the rubric + the runtime safety report, and the chronicler (work,tier,chapter) keying is consistent across the agent + the runtime artifacts + the kb-formats §4 contract, with adversarial framing "Assume at least 10 cross-phase inconsistencies (a count mismatch, an unconsumed drift, a schema disagreement). Find them.", producing `qa-post-completion-internal-consistency-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa agent with the evidence-quality lens to review the COMPLETE tree, checking that every spec-derived claim across all 4 phases cites a verifiable spec line, every carried-verbatim file is byte-faithful to its source, the boundary hashes are computed (not hand-authored), and no fabricated paths/SHAs/examples exist anywhere, with adversarial framing "Assume at least 10 evidence-quality errors (an uncited claim, an unfaithful carry, a fabricated example). Find them.", producing `qa-post-completion-evidence-quality-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PC.3:** Spawn content lens agents (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa-qualitative agent with the actionability lens to review the COMPLETE tree as a contributor-facing framework, checking that CLAUDE.md lets a new contributor unambiguously classify any file, VENDOR.md + UPSTREAM-SYNC.md give executable sync/enforcement commands, the boundary-contract note tells a contributor exactly what to do if they think they must edit an adopted file ("STOP. Add a skill instead"), and the Phase-3 hard-gate report's RED path (if exercised) is actionable, with adversarial framing "Assume at least 10 actionability gaps across the framework (an ambiguous classification, a missing command, an unactionable gate). Find them.", producing `qa-post-completion-actionability-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the boundary-contract-fidelity lens to perform the FINAL boundary assertion: run `uv run python laf-adaptation/scripts/check_boundary.py` and confirm exit 0, then independently verify all 6 rules hold on the complete tree (A laf-hash match for all 23 adopted files + resources; B ADOPTED-CLEAN == upstream-after-rewrite; C writer diff frontmatter-only+additive; D quartet intact + editor never folded; E no native/build-new name collides with upstream; F every agents/*.md + skills/**/SKILL.md is manifest-listed), with adversarial framing "Assume at least 10 boundary-contract breaches latent in the complete tree (a drifted adopted file, a missing manifest row, a folded editor, a name collision). Find them by running the script AND exhaustive manual spot-checks.", producing `qa-post-completion-boundary-fidelity-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative agent with the source-fidelity lens to verify the COMPLETE carried-verbatim corpus (all 9 port-source files: 4 tier YAMLs + universal-mappings + thematic + character + work-mapping-template + the Tolkien instance) are byte-faithful to their LAF port-source, reading both source and target for each and confirming byte-identity (allowing only the documented rename + `_`→`-` + the fenced-wrapper prose), with adversarial framing "Assume at least 10 fidelity breaches across the corpus (a normalized key, an altered value, a paraphrased subroutine). Find them by exhaustive byte comparison.", producing `qa-post-completion-source-fidelity-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.

**Step PC.4:** Consolidate post-completion findings and apply fixes (serialized per I20)
- [x] Read all 6 post-completion QA reports from Steps PC.2-PC.3, consolidate into `qa-post-completion-consolidated-findings.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/qa-post-completion-consolidated-findings.md` listing every issue deduplicated with severity + originating lens. Once done, mark this item as complete.
- [x] Spawn ONE rf-qa fix agent with `fix_authorization: true` and the consolidated findings as input, applying ALL fixes to the assembled tree (NEVER editing an adopted body; re-porting a YAML byte-faithfully; fixing a native/build-new artifact), producing a fix-applied summary. Once done, mark this item as complete.

**Step PC.5:** Post-completion verification round (PARALLEL, fix_authorization: false)
- [x] Spawn an rf-qa verification agent to confirm all post-completion findings were addressed, the boundary gate is still exit 0, and no new issues were introduced, producing `qa-post-completion-verification-structural-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Spawn an rf-qa-qualitative verification agent to confirm content + fidelity quality maintained after the fixes, producing `qa-post-completion-verification-content-report.md` at `.dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/qa/` with `fix_authorization: false`. Once done, mark this item as complete.
- [x] Read the two post-completion verification reports. IF both PASS, the final assembled-output gate PASSES — the tree is release-ready. IF either FAIL, repeat Steps PC.4-PC.5 (max 3 cycles per I16 report-validation gate); if unresolved after 3 cycles, HALT and escalate, logging in ### Phase Gate Findings. Once done, mark this item as complete.

**Step PC.6:** POST reflect gate (penultimate item — runs the deep reflect audit before marking Done)
- [x] Run the POST reflect gate as a flat wrapper shell-out by using the Bash tool to execute the command: `if [ "${SUPERCLAUDE_REFLECT_WRAPPER_ACTIVE:-0}" = "1" ]; then echo "reflect-wrapper recursion breaker: nested gate suppressed"; exit 0; fi; superclaude reflect run ".dev/tasks/to-do/TASK-RF-laf-hybrid-build-20260703-115948/TASK-RF-laf-hybrid-build-20260703-115948.md" --depth deep --fix` and capture the exit code. (NOTE on the `--promote` flag: for LAF 0.1, run AUDIT-ONLY without `--promote` — the Phase-3 hard gate and the final assembled-output QA gate (PC.2-PC.5) have ALREADY verified the executed tree, and a `--fix --promote` auto-mutation made AFTER those gates would not be re-verified by them. `--fix` is retained so reflect's bounded audit→apply→re-verify loop can resolve trivial deviations, but `--promote` is omitted for 0.1 so the task is not moved to `done/` and no unchecked mutation lands after the gates. If the wrapper refuses to run without `--promote`, fall back to the bare `--depth deep` form.) Handle the result: IF exit code 0 (reflect PASSED — coverage adequate, no deviations, or trivial deviations auto-fixed and re-verified by the wrapper's own bounded loop), proceed to mark this item complete and continue to the status-update item; IF exit code 10, 11, OR 2 (reflect found unresolved deviations / coverage gaps that could not be auto-fixed, OR a reflect infrastructure error occurred — e.g. exit 10/11 unresolved deviations/coverage gaps, exit 2 the superclaude CLI is unavailable or the reflect subagent could not spawn), treat ALL of these as a FAIL — write the reflect verdict + report path to `reflect_post` in the frontmatter (per the wrapper's write-back), log the deviation/infrastructure error in ### Phase Gate Findings, and HALT (do NOT mark the task Done; escalate). ONLY exit 0 proceeds; every non-zero exit (10, 11, AND 2) FAILs — an infra error (exit 2) is surfaced as a FAIL/escalation, NOT silently bypassed. Ensure the task file's `reflect_post` field is left for the wrapper to write back (do NOT hand-author the verdict). Once done, mark this item as complete.

- [x] Create a ### Task Summary section at the top of the ## Task Log / Notes section at the bottom of this task file, using the templated format provided there. The summary should document: work completed (the complete `laf-adaptation/` tree — 15 agents, 16 skills, native kb layers, scripts/check_boundary.py, VENDOR.md + NOTICE + LICENSE-CWS + CLAUDE.md + UPSTREAM-SYNC.md + .githooks + CI; the Phase-3 hard gate GREEN — full 11-step T1 run + T1/3/5 fan-out, all 5 conditions passed), challenges encountered (upstream CWS acquisition + SHA pinning, proof-chapter provisioning path chosen, any QA fix cycles), deviations from the planned process and their rationale (e.g. PD stand-in vs external Tolkien input, any tier_N.md count decision), and blockers logged during execution with their resolution status. Once the summary is complete, mark this item as complete.

- [x] Update `completion_date` to "2026-07-03" and `updated_date` to today's date, and update task `status` to "🟢 Done" in frontmatter, then add an entry to the ### Execution Log in the ## Task Log / Notes section at the bottom of this task file using the format: `**[YYYY-MM-DD HH:MM]** - Task completed: Updated status to "🟢 Done" and completion_date.` — only after the POST reflect gate (Step PC.6) has PASSED (exit 0 only — exit 10/11/2 are FAILs that HALT and escalate, NOT non-blocking), AND the final assembled-output QA gate (Steps PC.2-PC.5) has PASSED, AND the Phase-3 hard gate is GREEN. If any of those is not yet true, do NOT mark Done — leave status "🟠 Doing" and document the blocker. Once done, mark this item as complete.

## Task Log / Notes 📋

### Task Summary

**Completion Date:** 2026-07-04

**Work Completed:** The complete `laf-adaptation/` single-tree distribution (LAF 0.1, CWS Hybrid Path C) was built end-to-end across Phases 0–4, and all gates passed.
- **Phase 0 (Vendor):** vendored the 23 adopted units (11 agents [10 ADOPTED-CLEAN + `writer.md` ADOPTED-PATCHED] + 12 skills, 56 files recursively) patch-clean from the pinned upstream CWS checkout (`3338495f…`) via the deterministic prefix rewrite; authored `scripts/check_boundary.py` (the only script), `VENDOR.md` (64-row manifest), `NOTICE`, `LICENSE-CWS`, `CLAUDE.md`, `.githooks/pre-commit`, `.github/workflows/boundary.yml`; `--init` + verify green.
- **Phase 1 (Native Spine):** 3 native skills (`adaptation-tiers` +5 tier commentaries, `adaptation-rules` +thematic/character/agency, `source-fidelity`), 2 native agents (`analyst` opus / `safety-verifier` sonnet), the `writer.md` graft (Rule C green), and 5 byte-faithful kb ports (tier_1/2/3/5 + universal-mappings) + the work-mapping template.
- **Phase 2 (Greenfield):** 2 build-new agents (`chronicler` sonnet / `tier-coordinator` opus), the `/adaptation-safety` skill + `children.md`/`ya.md` genre resources, and the native kb adaptation-layer scaffold.
- **Phase 3 (HARD GATE):** full 11-step workflow on the proof chapter at Tier 1 (CONVERGED, safety PASS), then the T1/3/5 fan-out via tier-coordinator (RECONCILED, conflicts:[]). **All 5 gate conditions GREEN** → the integration test passed ("0.1 done").
- **Phase 4 (Upstream-Sync):** authored `UPSTREAM-SYNC.md` (6-step reconcile procedure, self-contained commands).
- **Handoff files:** `phase-outputs/{discovery,test-results,reviews,reports,plans}/**` (inventory, boundary run captures, gate-condition verdicts, per-phase + hard-gate + final-assembled-tree summaries); `qa/**` (all lens reports + consolidated findings + fix-applied summaries for 5 QA gates); `reflect/post/**`.

**Challenges Encountered:**
- Upstream acquisition: a pre-existing pinned CWS checkout was reused as the `--upstream` source (gitignored, outside Rule-F glob), avoiding a network clone.
- `check_boundary.py` spec ambiguities (verify-without-upstream rule set; NATIVE-vs-BUILD-NEW classification; manifest granularity; cwd-rooting) — resolved with the safest reading and documented; later verified by negative tests (cwd-rooting from /tmp → exit 0; Rule-F rogue-file → exit 1).
- Proof chapter: Tolkien is not public domain → provisioned an ORIGINAL SYNTHETIC Tolkien-adjacent fixture (PATH B, zero copyrighted prose) aligned to the committed Tolkien work-mapping.
- 5 QA gates each surfaced findings (mostly design-level under-specifications the specs themselves carry, or aspirational-tense doc staleness) — each resolved in 1 fix cycle via a single serialized fix agent that never touched an adopted body or a carried-verbatim payload.

**Deviations from Process:**
- **Proof-chapter provisioning (Step 5.1):** PATH B synthetic fixture instead of a real Tolkien chapter — rationale: constraint #6 forbids committing copyrighted prose; PATH A (operator input) unavailable in an autonomous run. Proves pipeline mechanics (the hard gate's purpose).
- **QA-gate fixes:** design-level executability gaps (e.g. T4 interpolation for non-numerics, tier-coordinator maturity()/disclosure semantics) were resolved with additive, spec-consistent "operational-reading" notes rather than inventing design beyond spec, to preserve the byte-faithful carry — documented in each gate's findings.
- **PC.6 POST reflect gate:** exit 11 (degraded-tier1) — an environmental Tier-2-unavailability, NOT a content defect (audit clean, 0 deviations). Escalated per the strict contract; the operator authorized "Accept & mark Done" (recorded in Phase Gate Findings 2026-07-04 02:00).

**Blockers Logged:**
- **Step PC.6:** POST reflect gate exit 11 (degraded-tier1; Tier-2 reviewer models unavailable in this environment) - **Status:** Resolved by authorized operator override (content audit clean; all functional gates GREEN; reviewer verification gaps closed).
- No other blockers; every prior step completed with evidence.

**Follow-Up Required:** Optional - (1) re-run the POST reflect gate in an environment with Tier-2 reviewer-model diversity to obtain a clean non-degraded exit-0 pass (the content verdict is already clean). (2) The reflect reviewer's stress-tests (F2 cwd-rooting, F3 Rule-F rogue-file) are already closed with passing negative tests; consider adding them as permanent regression checks. (3) For a real adaptation run, swap the PATH-B synthetic fixture for a PATH-A operator-supplied chapter.

### Execution Log

<!-- TEMPLATE FOR EXECUTION LOG ENTRIES:
**[YYYY-MM-DD HH:MM]** - [Action taken]: [Brief description of what was done and outcome]
-->

**[2026-07-03 21:41]** - Task started: Updated status to "🟠 Doing" and start_date.

**[2026-07-04 02:05]** - Task completed: Updated status to "🟢 Done" and completion_date. All build phases 0-4, all 4 phase-gate QA gates, the Phase-3 hard gate (5/5 GREEN), and the final assembled-output QA gate PASSED; the POST reflect gate degraded to Tier-1 (environmental Tier-2 unavailability) with a CLEAN content audit (0 deviations) and was accepted by authorized operator decision.

### Phase 2 - Phase 0 Vendor Findings

<!-- TEMPLATE FOR PHASE FINDINGS:
**[YYYY-MM-DD HH:MM]** - [Step X.Y]: [Finding or blocker description]
- **Status:** [Completed | Blocked]
- **Details:** [Specific information about what was found, created, or what blocked completion]
- **Blocker Reason (if blocked):** [Specific reason why this could not be completed]
- **Files Affected:** [List of files read, created, or modified]
-->

**[2026-07-03 21:41]** - Step 1.2: First git commit on `main` + `start_commit` repin.
- **Status:** Completed
- **Details:** Repo `main` confirmed at zero-commit state (`git rev-parse --verify HEAD` failed as expected). Added `.upstream-cws/` to `.gitignore` (Phase-0 upstream checkout dir, must stay outside Rule-F glob). Discovered a pre-existing embedded CWS checkout at `.dev/releases/current/0.1/creative-writing-skills` (nested git repo pinned at SHA `3338495f0fabf778720effdda9386ab56d4ebf6e`, ships both `cw/` and `agents/` subtrees) — force-unstaged it (`git rm -rf --cached`, working tree preserved) so it is NOT committed as a broken gitlink, and added it to `.gitignore`. **This checkout is a candidate upstream source for Step 2.1 (already pinned, has `cw/agents/` + `cw/skills/`), potentially avoiding a fresh network clone.** Made the baseline commit `bf8d8b1a9ced25753f14e2bf9426bb235f666c2c` (89 files: design pack + port-source config/prompts/templates + task file + docs). Repinned frontmatter `start_commit:` from the sentinel to the real 40-char SHA. The reflect wrapper (Step PC.6) resolves its audit base (the commit to diff against) from this `start_commit:` field, so a resolvable SHA is load-bearing for the POST reflect gate.
- **Files Affected:** `.gitignore` (created baseline entries), this task file frontmatter `start_commit:`, git index (baseline commit).

**[2026-07-03 21:41]** - Step 2.2: Authored `laf-adaptation/scripts/check_boundary.py` (spec-ambiguity resolutions documented per item instruction).
- **Status:** Completed
- **Details:** Pure-stdlib Python, `py_compile` clean, `--help` verified. Resolved these under-specified points with the safest reading: (1) **verify without `--upstream`** runs Rules A (hash match) + D (quartet) + E (manifest-only collision) + F (coverage); Rules B/C (upstream-diff) are skipped with a printed NOTE — matches the CI/pre-commit usage (Steps 2.11/2.10) which run verify with NO `--upstream`, and Rule A already catches any adopted-file drift from the recorded manifest. (2) **`--init` full-derivation:** computes `upstream_sha256` = sha256(raw upstream blob) and `laf_sha256` = sha256(on-disk vendored file), then runs a post-write `verify(upstream)` self-check so `--init` green proves the prefix rewrite is the only transform. (3) **Manifest granularity:** ADOPTED files get one concrete per-file row (SKILL.md + every `resources/**` recursively → Rule A/B catch any resource body edit); NATIVE/BUILD-NEW skills get a single `skills/<name>/**` glob row (— hashes), matching boundary-contract.md §2 lines 74-81. `manifest_covers()` treats a `/**` row as a prefix glob so Rule F finds native SKILL.md covered. (4) **NATIVE vs BUILD-NEW classification:** a documented closed provenance map (`NATIVE_AGENTS`/`BUILD_NEW_AGENTS`/`NATIVE_SKILLS`/`BUILD_NEW_SKILLS`, per constraint #8) labels newly-discovered non-upstream files; an existing manifest row's class is ALWAYS preserved over the map (manifest = source of truth). (5) **Path rooting:** `REPO = Path(__file__).resolve().parents[1]` = `laf-adaptation/`; all globs/paths resolve against it regardless of caller cwd (the cwd-rooted-verifier Open Question). `writer.md` hardcoded → ADOPTED-PATCHED.
- **Files Affected:** created `laf-adaptation/scripts/check_boundary.py`.

### Phase 3 - Phase 1 Native Spine Findings

**[2026-07-03 23:05]** - Step 3.10: writer.md ADOPTED-PATCHED graft confirmed end-to-end.
- **Status:** Completed
- **Details:** All four graft properties verified: (a) `skills:` list is the prefix-rewritten upstream list + exactly one appended `- laf-adaptation:adaptation-rules`; (b) the upstream duplicate `creative-writing-craft` line is preserved (count==2); (c) body byte-identical to upstream-after-rewrite, zero removed frontmatter lines, zero residual old-prefix; (d) VENDOR.md classes it ADOPTED-PATCHED with two distinct hashes (`373e605b…` upstream / `c1b3e12f…` laf). **Rule C PASSES** — writer.md is absent from the violation list. The full-verify run returned exit 1, but ONLY for Rule F on the 5 not-yet-manifested native files (analyst, safety-verifier, adaptation-tiers/-rules/source-fidelity SKILL.md) — these were authored in Steps 3.1-3.9 and are manifested by Step 3.18 (`--init`). This is the expected intermediate state, NOT a graft defect; no fix to the graft is needed. Verified again at Step 3.19 after the manifest update.
- **Files Affected:** read laf-adaptation/agents/writer.md, VENDOR.md (no modification).

### Phase 4 - Phase 2 Greenfield Findings

### Phase 5 - Phase 3 Hard Gate Findings

**[2026-07-03 23:50]** - Step 5.1: Proof chapter provisioned via PATH B (committable stand-in) — assumption documented.
- **Status:** Completed
- **Details:** No operator-supplied real Tolkien chapter is available in this autonomous run, and constraint #6 forbids committing copyrighted Tolkien prose. Chose **PATH B**: authored `source/tolkien/ch-01.txt` as an **ORIGINAL SYNTHETIC proof fixture** (LAF-authored original prose, ~250 words, NOT Tolkien's text), written Tolkien-adjacent so it aligns with the committed Tolkien work-mapping and exercises every transform class the hard gate must prove: violence→cooperation (the battle/siege), agency externalization (Sauron's "cold patient will"; the dark lord as a force), death handling (Théoden falls; "many good soldiers had fallen and would not rise"), martial→prosocial heroism (Éowyn stands over the fallen king), and nightmare framing (the Nazgûl's cry). `work` = `tolkien` so the work-mapping instance is `tolkien-mapping.yaml` (a byte copy of the committed `config/concept_mapping/templates/tolkien_mapping.yaml`, satisfying the PC.3 fidelity check). `source/README.md` documents the PATH-A/PATH-B contract and labels the fixture synthetic.
- **Assumption:** the proof runs on a synthetic Tolkien-adjacent fixture (PATH B), NOT real Tolkien prose. This proves pipeline MECHANICS (the Phase-3 hard gate's purpose), not literary output on the actual work. A real run would swap in a PATH-A operator chapter. Zero copyrighted prose committed.
- **Files Affected:** created `laf-adaptation/source/README.md`, `laf-adaptation/source/tolkien/ch-01.txt`.

**[2026-07-04 00:05]** - Steps 5.3-5.11: Phase-3 HARD GATE — **GREEN (all 5 conditions PASS). "0.1 is DONE."**
- **Status:** Completed
- **Details:** Executed the full 11-step workflow on ch-01 at Tier 1 (CONVERGED: analyst→muse→writer→critic→editor→writer-revision→continuity-checker→safety-verifier→reader-sim→tier-coordinator→chronicler), then the T1/3/5 fan-out via tier-coordinator (RECONCILED, conflicts:[]). During v2 revision a Section-6 passive-voice issue was caught and corrected (exercised the ordinary revision path; safety passed first blocking check so the FAIL-loop wasn't triggered, but its wiring is present in safety-verifier.md). **5 gate conditions:** (1) v2.0 tags present ✅; (2) safety PASS (blocking, all 6 sections) ✅; (3) per-tier canon written T1/T3/T5 with (work,tier,ch) keying + no shared-canon leak ✅; (4) all 4 quartet agents ran + editor never folded ✅; (5) check_boundary.py exit 0 both modes after the run ✅. Tier differentiation proven (Sauron→"Grumpy King"/T1, "Sauron"/T3-T5; death→"rest"/T1, "died"/T3, "he fell"/T5); Inv.3 verified by grep (no transformed name in shared kb/canon/). Consolidated report: `phase-outputs/reports/phase-3-hard-gate-report.md`.
- **Files Affected:** 20+ runtime artifacts under `laf-adaptation/work/` and `laf-adaptation/kb/` (analysis, cross-tier, 4 drafts, 4 critique reports, 2 safety reports, shared canon+timeline, per-tier continuity/decisions/canon-delta/adapted/analysis ×3 tiers); 6 gate-condition + run-summary + review reports under phase-outputs/.

### Phase 6 - Phase 4 Upstream-Sync Findings

### Phase Gate Findings

_QA gate verdicts, fix cycle counts, and unresolved issues are recorded here._

**[2026-07-03 22:30]** - Phase Gate 0 (Phase-0 Vendor QA) — **PASSED** after 1 fix cycle.
- **7 lens agents (PG0.2-PG0.3):** 6 PASS with 0 issues (template-conformance, internal-consistency, evidence-quality, boundary-contract-fidelity, source-fidelity, domain-accuracy); 1 FAIL — the actionability lens found 7 doc-actionability issues (2 CRITICAL, 3 IMPORTANT, 2 MINOR) in the two NATIVE docs CLAUDE.md/VENDOR.md. The boundary gate itself was GREEN across all lenses (`check_boundary.py` exit 0; A–F hold); no adopted-file or fidelity defect.
- **Fix cycle 1 (PG0.4):** consolidated to `qa/qa-consolidated-findings.md`; ONE fix agent applied F1–F7 to CLAUDE.md + VENDOR.md only (UPSTREAM-SYNC forward-ref annotation; hook enable/bypass commands; non-manifested-files classification note; target-count annotation; CI-at-repo-root clarification; VENDOR re-vendor pointer; source/templates "created later" annotation). Adopted files untouched; manifest table byte-unchanged; post-fix `check_boundary.py` exit 0.
- **Verification (PG0.5):** both agents PASS — structural 7/7 addressed + boundary exit 0; content 0 residual issues. Root cause of the FAIL was CLAUDE.md describing the target complete tree while Phase 0 is a partial state; resolved by making present-vs-planned explicit + adding two missing command pointers.
- **Verdict:** Phase-0 QA gate GREEN. Proceed to Phase 1 (Native Spine).

**[2026-07-03 23:20]** - Phase Gate 1 (Phase-1 Native Spine QA + M4 source-fidelity gate) — **PASSED** after 1 fix cycle.
- **10 lens agents (PG1.2-PG1.4):** 9 PASS with 0 issues (template-conformance, internal-consistency, evidence-quality, completeness, boundary-fidelity, domain-accuracy, + all 3 M4 fidelity: kb-YAML byte-faithful, skill-resource-wrapper byte-faithful, cross-source no-contradiction); 1 FAIL — actionability found 7 design-level under-specifications (1 CRITICAL T4-interpolation-for-non-numerics, 4 IMPORTANT, 2 MINOR). All 9 carried-verbatim ports confirmed byte-identical, so the FAIL was NOT a carry defect.
- **Fix cycle 1 (PG1.5):** consolidated 10 reports to `qa/qa-fidelity-consolidated-findings.md`; ONE fix agent applied F1-F7 as ADDITIVE actionability clarifications to the 5 native prose bodies only (interpolation-by-threshold-type; schema-tolerant step-2; adaptation-safety Phase-2 forward-ref note; access-level observable-test rule; deterministic safety `next` rule; authoritative-contract + overall-confidence notes). NO carried-verbatim YAML payload, kb file, template, or adopted file touched; post-fix `check_boundary.py` exit 0.
- **Verification (PG1.6):** both agents PASS — structural 7/7 addressed + all 9 carries still byte-identical (sha256 match M4 baseline) + boundary exit 0; content 0 residual, F7 genuinely resolved, no Mars keys, additions clearly marked operational-readings not spec rewrites. tolkien-mapping.yaml fidelity DEFERRED to PC.3 (created Phase 3).
- **Verdict:** Phase-1 QA + M4 fidelity gate GREEN. Proceed to Phase 2 (Greenfield).

**[2026-07-03 23:40]** - Phase Gate 2 (Phase-2 Greenfield QA) — **PASSED** after 1 fix cycle.
- **7 lens agents (PG2.2-PG2.3):** 4 PASS (template-conformance, completeness, boundary-fidelity, domain-accuracy); 3 FAIL — internal-consistency (5: chronicler omits analysis.yaml promotion; adaptation-safety `next` diverges from safety-verifier), evidence-quality (2: children.md Denethor T3 example was a stitched quote presented as verbatim), actionability (6, mostly design-level: tier-coordinator maturity()/disclosure abstraction, in-tree workflow reference). Boundary gate GREEN throughout.
- **Fix cycle 1 (PG2.4):** consolidated to `qa/qa-consolidated-findings-phase2.md`; ONE fix agent applied 4 genuine build defects (D1 children.md full-verbatim T3 quote; D2 attribution; D3 chronicler analysis.yaml promotion-on-accept; D4 adaptation-safety `next` aligned to safety-verifier F6) + 3 spec-consistent operational-reading notes (E1 tier-coordinator maturity/disclosure grounded in tier-profile fields; E2 chronicler key-materialization + Inv.3 name-check; E3 11-step workflow pointer). Edited only the 4 BUILD-NEW native bodies; NO carried-verbatim payload, kb, template, or adopted file touched; post-fix `check_boundary.py` exit 0.
- **Verification (PG2.5):** both agents PASS — structural 7/7 addressed + boundary exit 0 + no regression (carried files mtime-confirmed untouched); content 0 residual, D1 byte-faithful verbatim confirmed, E1/D4 accurate against tier_1.yaml/safety-verifier. Design-level abstractions (maturity/disclosure semantics) resolved via operational-reading notes without inventing beyond spec.
- **Verdict:** Phase-2 QA gate GREEN. Complete tree = 15 agents + 16 skills, boundary green. Proceed to Phase 3 (HARD GATE — the definition of "0.1 done").

**[2026-07-04 00:40]** - Phase Gate 3 (Phase-3 Hard-Gate QA) — **PASSED** after 2 fix cycles; hard-gate stays GREEN.
- **7 lens agents (PG3.2-PG3.3):** 4 PASS (template-conformance, evidence-quality, completeness, boundary-fidelity), 3 FAIL — internal-consistency (quotation drift), domain-accuracy (1 CRITICAL: source fixture leaked the T1 label "Grumpy Riders" + analyst death self-contradiction; T3 hardened an ambiguous death), actionability (reports not self-contained on failure paths). Boundary-fidelity + provenance clean; the 5 gate conditions stayed genuinely GREEN throughout.
- **Fix cycle 1 (PG3.4):** ONE fix agent applied D1-D8 — de-contaminated the source fixture (removed "Grumpy Riders", made Théoden's death explicit "did not rise again"), fixed the analysis.yaml death contradiction, aligned the T5 near-source draft, resolved the T1 quotation drift, made the fan-out summary honest about T3/T5 (only T1 ran the full quartet), fixed the stale raw-capture FAIL line, added failure-path notes + wording/traceability fixes. No adopted/carried-verbatim file touched; boundary exit 0; hard-gate GREEN.
- **Fix cycle 2 (executor, PG3.5 residuals):** the content verification agent found 3 MINOR residual T5-quote citation drifts (D3 changed the T5 draft but the T5 quote wasn't swept across 3 reporting artifacts) + 1 related "steps 3-9" overstatement in cross-tier.md — all 4 fixed directly (deterministic citation swaps); boundary still exit 0.
- **Verification (PG3.5):** both agents PASS — structural 8/8 addressed + hard-gate GREEN + boundary exit 0; content source-fidelity chain coherent end-to-end (source "did not rise again" → analysis CERTAIN → T3 "died" → T5 preserves → T1 "rest"), invariants hold, no regression. Post-residual-fix grep confirms all T5 citations aligned.
- **Verdict:** Phase-3 QA gate GREEN; hard-gate GREEN. Proceed to Phase 4 (Upstream-Sync doc).

**[2026-07-04 01:30]** - Final Assembled-Output Gate (Post-Completion PC.2-PC.5) — **PASSED** after 1 fix cycle.
- **6 lens agents (PC.2-PC.3) on the COMPLETE tree:** 4 PASS with 0 issues (template-conformance, evidence-quality, boundary-contract-fidelity [the FINAL boundary assertion — all 6 rules independently re-derived, exit 0 both modes], source-fidelity [all 9 carried-verbatim files + agency.md byte-faithful]); 2 FAIL — internal-consistency (7) and actionability (10). **The tree's structure, byte-fidelity, and boundary contract are perfect;** the FAILs were documentation-accuracy: CLAUDE.md/UPSTREAM-SYNC.md were authored/annotated in aspirational Phase-0 tense and had gone STALE now that 0.1 is DONE and the full 15-agent/16-skill tree exists (the map contradicted the territory).
- **Fix cycle 1 (PC.4):** ONE fix agent applied 8 groups (G1 present-tense rewrite of CLAUDE.md counts+tree; G2 adopted-resources-tree classification rule + adopted-count + manifest-vs-glob clarifications; G3 design-pack-provenance note; G4 UPSTREAM-SYNC Steps 1/6 made self-contained/executable; G5 adaptation-safety `next` inline pointer; G6 tier-axis `active_tier` correction; G7 analyst status enum note; G8 hard-gate RED-path for all 5 conditions). Edited only CLAUDE.md, UPSTREAM-SYNC.md, analyst.md (prose), adaptation-safety SKILL.md (inline comment), and the hard-gate report. NO adopted/carried-verbatim file touched; boundary exit 0.
- **Verification (PC.5):** both agents PASS — structural 8/8 groups addressed + boundary exit 0 + hard-gate GREEN + no regression (VENDOR.md manifest unchanged, Rule A hash-match green); content 0 residual (counts 15/16 accurate, tier-axis correction verified against writer.md/tier-coordinator.md, UPSTREAM-SYNC commands executable). (One content-verifier attempt timed out; a fresh retry completed with PASS.)
- **Verdict:** Final assembled-output gate GREEN — the tree is release-ready. Proceed to PC.6 (POST reflect gate).

**[2026-07-04 01:55]** - Step PC.6: POST reflect gate — **exit 11 (degraded-tier1) = FAIL per contract → HALT + ESCALATE (task NOT marked Done).**
- **Command:** `superclaude reflect run <task> --depth deep --fix` (audit-only, no `--promote`, per PC.6 note for 0.1). superclaude v4.3.5; audit base `start_commit bf8d8b1a9ced…` (resolvable). Output: `phase-outputs/reports/post-reflect-output.txt`.
- **Result:** exit **11**, verdict `degraded (degraded-tier1)`. The wrapper wrote `reflect_post:` back into the frontmatter (verdict: degraded, tier_reached: 1, reason: degraded-tier1). **The degradation is purely ENVIRONMENTAL:** the return-contract shows `status: success`, `tier_reached: 1`, `reviewer_count: 1`, `merge_method: single-reviewer-fallback`, `t2_model_class_diversity: insufficient`, `verification_skip_reason: tool-unavailable` — the Tier-2 heterogeneous reviewer swarm could not run in this environment and degraded to a single Tier-1 reviewer. Per the reflect wrapper's own rule ("only a clean, full, non-degraded Tier-2 pass exits 0"), a degraded run cannot exit 0 by design, independent of content quality.
- **Content verdict is CLEAN:** `deviation_count_by_class` = authorized 0 / necessary 0 / **drift 0 / regression 0**; `regression_present: false`; `unauthorized_deviation_present: false`; `needs_human_decision: false`. The Tier-1 reviewer report concludes: *"The build is technically sound and passes all functional gates"* (boundary contract GREEN across Phases 0-3 & PC.5; Phase-3 hard gate 5/5 GREEN).
- **Reviewer findings triaged + closed:** F1 (frontmatter still "Doing"/PC.6 unchecked) = EXPECTED mid-gate state (the reviewer itself notes reflect_post is "correctly left empty per Step PC.6"). F2 (HIGH, no cwd-rooting negative test) → **CLOSED:** ran `check_boundary.py` from `/tmp` → exit 0 (rooting via `Path(__file__).resolve().parents[1]` works regardless of cwd). F3 (HIGH, no Rule-F rogue-file test) → **CLOSED:** injected an unmanifested `skills/rogue-test/SKILL.md` → exit 1 "not in VENDOR.md manifest" (Rule F catches it); removed → exit 0. F4 (LOW, fixture-entity alignment) → **CONFIRMED:** Sauron/Denethor resolve against tolkien-mapping.yaml; "Nazgûl" (fixture) ↔ "nazgul" (mapping key) same entity (diacritic variance only).
- **Blocker Reason:** the POST reflect gate cannot achieve a clean non-degraded Tier-2 pass in this environment (Tier-2 external reviewer models unavailable → single-reviewer fallback → exit 11). This is an infrastructure/environment limitation, NOT a work-quality defect: all functional gates (Phases 0-3, hard gate, final assembled-output QA) are GREEN and the reflect content audit found 0 deviations/regressions.
- **Action:** per the PC.6 contract, HALTED before marking Done and ESCALATED to the operator for a decision (accept the degraded-tier1 reflect and mark Done, vs re-run reflect in an environment with Tier-2 reviewer models).
- **[2026-07-04 02:00] OPERATOR DECISION:** the operator reviewed the escalation and chose **"Accept & mark Done"** — accepting the degraded-tier1 reflect. Rationale on record: the degradation is purely environmental (Tier-2 reviewer models unavailable), the reflect content audit is clean (0 deviations/regressions), every functional gate (Phases 0-4 + Phase-3 hard gate 5/5 + final assembled-output QA) is GREEN, and the reviewer's 2 HIGH verification gaps (F2 cwd-rooting, F3 Rule-F) were closed with passing negative tests. This is an authorized override of the strict exit-11 HALT for LAF 0.1. PC.6 marked complete; proceeding to mark the task 🟢 Done.

### Open Questions (documented at build time — execution-time decisions / assumptions)

These could NOT be resolved by research and are carried as risks/assumptions, NOT as basis for task items:

- **Upstream CWS commit SHA to pin (`upstream_sha`):** an execution-time choice. The task acquires "first" per the user decision, but the exact SHA is picked at execution (Step 2.1). VENDOR.md holds it; `check_boundary.py --init` records it.
- **Proof-chapter provisioning:** real Tolkien is NOT public domain (still under copyright in most jurisdictions; US expiry well after 2026). Step 5.1 supports an execution-time external input (PREFERRED — operator supplies prose under their own access rights) OR a public-domain stand-in (committable fallback). Do NOT commit copyrighted Tolkien prose. The mapping is seeded from `config/concept_mapping/templates/tolkien_mapping.yaml` (a mapping config, not prose).
- **License filename for the vendored Apache-2.0 copy:** `LICENSE` vs `LICENSE-CWS` — Step 2.9 uses `LICENSE-CWS` for clarity (recommendation); confirm at execution.
- **Hook/CI working-directory:** whether `.githooks/pre-commit` needs a `cd` and the CI workflow needs `working-directory: laf-adaptation` depends on whether `laf-adaptation/` is the git root (it is NOT here — repo root is `/config/workspace/Infantalizer`, so the wrappers must account for the nested path; Steps 2.10-2.11 flag this).
- **Verify-mode upstream access:** spec pins `--upstream` only to `--init`; whether verify re-fetches upstream is unstated. Safest reading (encoded in Step 2.2): verify uses recorded `upstream_sha256` (Rule A + stored hashes), full re-derivation at `--init`.
- **tier_N.md commentary count:** 5 files recommended (tier_1/2/3/4/5.md — spec range says "tier_1.md … tier_5.md"; tier_4.md documents interpolation, must NOT be a stored profile); 4-file fallback is acceptable but weaker. Step 3.2 builds to 5.
- **[FROM PRE-REFLECT unmapped_requirements_union] POST-reflect auto-fix re-verification:** the PRE reflect gate flagged that a PC.6 `--fix --promote` auto-mutation would not be re-verified by the Phase-3 hard gate / final QA gate. Carried as an Open Question: resolved at build time by running PC.6 audit-only (`--fix` WITHOUT `--promote` for 0.1 — see Step PC.6); if a future version adopts `--promote`, add a Step PC.7 re-running gate-cond1..5 + `check_boundary.py` on the post-reflect tree.
- **[FROM PRE-REFLECT unmapped_requirements_union] cwd-rooted verifier contract:** the PRE reflect gate flagged that `check_boundary.py` uses relative globs but is invoked from repo root. Resolved at build time by requiring the script to root paths from `Path(__file__).resolve().parents[1]` (added to the Step 2.2 authoring spec); confirm at execution the rooting is implemented (it is asserted by the boundary-contract-fidelity QA lens).

### Follow-Up Items Identified

<!-- TEMPLATE FOR FOLLOW-UP ITEMS:
- **[Priority: High/Medium/Low]** [Description of follow-up needed] - Identified in Step [X.Y]
-->

### Deviations from Process

<!-- TEMPLATE FOR DEVIATIONS:
**[YYYY-MM-DD HH:MM]** - Deviation from [Step X.Y]:
- **Expected:** [What the process specified]
- **Actual:** [What was done instead]
- **Rationale:** [Why this deviation was necessary]
-->
