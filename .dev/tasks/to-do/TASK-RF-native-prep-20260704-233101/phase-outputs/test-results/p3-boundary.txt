# P3 Post-Run Boundary + Adopted-Body Gate (raw output)

After the P3 run (prep + rewrite delegation), from repo root.

## Command 1: `uv run python laf-adaptation/scripts/check_boundary.py 2>&1` (Mode V)

```
NOTE: verify running without --upstream — Rules B and C (upstream-diff checks) skipped; Rule A hash-match covers adopted-file integrity against the recorded manifest, plus Rules D/E/F.
BOUNDARY CONTRACT: PASS — all rules (A-F) satisfied.
```
Exit 0.

## Command 2: `git diff --stat -- laf-adaptation/agents/writer.md laf-adaptation/agents/muse.md 2>&1`

```
(empty)
```
