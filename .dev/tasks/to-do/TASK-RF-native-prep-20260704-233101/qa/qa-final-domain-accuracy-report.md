# QA Report — Final Consolidated Domain-Accuracy (Phase 6)

**Topic:** Native LAF Adaptation-Prep — domain-accuracy lens (meaning-preservation, dual-form promotion, prep pipeline)
**Date:** 2026-07-05
**Phase:** doc-qualitative (domain-accuracy overlay, Phase 6 final consolidated gate)
**Fix cycle:** N/A (report-only; `fix_authorization: false`)

**Lens scope.** Verify that the built outputs DESCRIBE LAF behavior that matches (a) the design pack at `docs/native-prep/design/` and (b) what the P3 run actually DEMONSTRATED (`p3-prep-run.md`, `p3-dualform-summary.md`, `p3-package-assert.md`, `p3-dualform.txt`, `p3-prerun-mapping-hashes.txt`, `p3-boundary.txt`, `p3-rewrite-assert.md`). Flag overclaims, contradictions, and mis-described behavior.

---

## Overall Verdict: FAIL

## Items Reviewed
| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | 8-file package described in built docs matches P3 run output (8 files, fixed names) | PASS | `work/prep/tolkien/` contains all 8 files `00..70`; sizes match shape. `prep/SKILL.md` §1 + `path-contract.md` §2 enumerate the same 8 fixed-name files; P3 `p3-prep-run.md` table lists all 8 with byte counts. |
| 2 | `30-mapping.yaml` 6-key with `meaning:` — described vs. run vs. on-disk | PASS | `package-schemas.md` §4 specifies 6-key form with `meaning:`; `p3-dualform.txt` (a) asserts 6 keys incl. `meaning`; on-disk `work/prep/tolkien/30-mapping.yaml` confirmed via `uv run pyyaml`: keys `['characters','concepts','key_scenes','master_translation_table','meaning','work_metadata']`, len 6. |
| 3 | kb hyphen copy keeps `meaning:` (6-key) — described vs. run vs. on-disk | PASS | `package-schemas.md` §4.1 + `path-contract.md` §3 specify hyphen 6-key keeps `meaning:`; `p3-dualform.txt` (b) PASS; on-disk `laf-adaptation/kb/adaptation-mapping/tolkien-mapping.yaml` confirmed 6-key with `meaning:`. |
| 4 | root underscore copy strips `meaning:` (5-key) — described vs. run vs. on-disk | PASS | `package-schemas.md` §4.1 + `path-contract.md` §3 specify underscore 5-key STRIPPED; `p3-dualform.txt` (c) PASS; on-disk `config/concept_mapping/templates/tolkien_mapping.yaml` confirmed 5-key, no `meaning:`. |
| 5 | Dual-form promotion non-vacuous (write-provenance) | PASS | `p3-prerun-mapping-hashes.txt` records pre-run sha256 (both targets byte-identical `914a85b…`); `p3-dualform.txt` (d) shows post-run hashes differ; `p3-prep-run.md` records post-run sha256 `2d62cb…` (root) and `b3368d…` (kb) — both differ from pre-run. Promotion genuinely re-wrote both targets. |
| 6 | Per-entry effective confidence = `min(text, context)` — described vs. run | PASS | `package-schemas.md` §4 Confidence-inheritance rule; `prep/SKILL.md` §4; `p3-package-assert.md` (c) programmatic pyyaml verification over every `_confidence` entry confirms `eff == min(rank(text), rank(context))`. |
| 7 | `20-analysis-work-level.yaml` carries `meaning` + `compound_scene` — described vs. run | PASS | `package-schemas.md` §3 schema; `p3-package-assert.md` (b) cites line 55 `meaning:` + line 63 `compound_scene: true`; on-disk file confirms both fields populated with source-grounded values. |
| 8 | `meaning:` is source-grounded, not invented | PASS | `20-analysis-work-level.yaml:55-61` grounds meaning in fixture-exhibited themes (hope against despair, redemptive courage); `p3-prep-run.md` "Fixture-driven notes" corroborates; `analyst.md` body rule "tagged with its own confidence" + `thematic-fidelity/SKILL.md` enforce data-tagging. |
| 9 | Stage-7 dual-form transform operator attribution in built docs | FAIL | `prep/SKILL.md` §6 headline says "promote … **via `/kb-management`**" then "Operator clarity" note says the **coordinator** owns the transform and `/kb-management` "does NOT". Same contradiction in `prep-cordinator.md` Stage 7 body and `path-contract.md` §5 (write-ownership table rows 73-74 say "Written by: `/kb-management`" but §5 note lines 80-85 say the coordinator is the operator). The `/kb-management` SKILL body confirms it has NO dual-form awareness. Headline mis-attributes the transform. |
| 10 | `thematic-fidelity` skill loadable by agents that reference it | FAIL | `analyst.md` body says "Grounded in `/thematic-fidelity`" but its `skills:` frontmatter lists only `source-fidelity, adaptation-tiers, story-memory` — NOT `thematic-fidelity`. `tier-coordinator.md` Check D header cites "(/thematic-fidelity)" and the body claim "no new skill line is required" — but `thematic-fidelity` is not in its `skills:` either (only `adaptation-tiers, source-fidelity, kb-management`). Per the boundary contract (CLAUDE.md §2), the only way for NATIVE knowledge to enter an agent is via `skills:` frontmatter; an unloaded `/thematic-fidelity` reference is unreachable at runtime. |
| 11 | `prep-cordinator` `tools:` line capabilities match actual dispatch | FAIL | `prep-cordinator.md:13` declares `Agent(web-researcher, analyst, tier-coordinator)` but the 8-stage prep body (lines 46-61) only dispatches `web-researcher` (Stage 1c) and `analyst` (Stage 3). `tier-coordinator` is a rewrite-phase step-10 agent and is never dispatched in any prep stage. Unused/overclaimed capability in the tools manifest. |
| 12 | tier-coordinator Check D filename reference is correct | FAIL | `tier-coordinator.md:97` says "Read the top-level `meaning:` from `<work>-mapping.yaml`". The 0.1 hyphen copy that KEEPS `meaning:` is `<slug>-mapping.yaml` (e.g. `tolkien-mapping.yaml`); the underscore form `<work>_mapping.yaml` is the root copy that STRIPS `meaning:`. Using `<work>-mapping.yaml` (with the bare `<work>` token) is ambiguous and, taken literally with the underscore convention from `path-contract.md` §3, points at the wrong (meaning-stripped) file. |
| 13 | P3 fixture substitution (Tolkien for Narnia) — described vs. run | PASS (with caveat) | DESIGN.md §7 P3 row specifies Narnia/narnia; run used Tolkien/tolkien. Authorized by `research/07-gap-fill.md` GAP 3 (no Narnia source ships). Built docs (`prep/SKILL.md`, `prep-cordinator.md`) use `<slug>` placeholders, so they do NOT carry the stale Narnia spec forward. The uncorrected DESIGN.md §7 row is a stale-spec finding outside this lens's built-output scope; flagged for completeness. |
| 14 | `/laf:rewrite` hardcoded read-set described vs. run | PASS | `path-contract.md` §4 `rewrite_phase_reads` lists 3 files; `commands/laf/rewrite.md` enumerates the same 3; `p3-rewrite-assert.md` A2 confirms all 3 read OK + `70-traceability.md` correctly NOT read. |
| 15 | `/laf:rewrite` greenlight guard described vs. run | PASS | `commands/laf/rewrite.md:20-21` guard; `path-contract.md` §4; `p3-rewrite-assert.md` A3 confirms `status: CONFIRMED` present; on-disk `50-greenlight.md` confirms `status: CONFIRMED`. |
| 16 | `muse` receives `meaning:` as DATA (no adopted-body edit) | PASS | `p3-boundary.txt` + `p3-boundary-summary.md` confirm `git diff --stat -- writer.md muse.md` EMPTY; `p3-rewrite-assert.md` A4 confirms no edit to muse, `meaning:` flows as data from `30-mapping.yaml:6`. |
| 17 | Boundary contract stays green after P3 run | PASS | `p3-boundary.txt` Mode V `BOUNDARY CONTRACT: PASS — all rules (A-F) satisfied`; design pack §5 VENDOR delta predicts +3 NATIVE rows; `final-output-inventory.md` confirms 3 NATIVE rows hand-added. |

## Summary
- Checks passed: 13 / 17
- Checks failed: 4
- Critical issues: 1
- Important issues: 3
- Issues fixed in-place: 0 (report-only)

## Confidence
- Verified: 17/17 | Unverifiable: 0 | Unchecked: 0 | Confidence: 100%
- Tool engagement: Read 11 (DESIGN.md, package-schemas.md, p3-prep-run.md, p3-dualform-summary.md, p3-package-assert.md, p3-dualform.txt, p3-prerun-mapping-hashes.txt, p3-boundary.txt, p3-rewrite-assert.md, prep/SKILL.md, prep-cordinator.md, analyst.md, tier-coordinator.md, kb-management/SKILL.md, thematic-fidelity/SKILL.md, path-contract.md, commands/laf/prep.md, commands/laf/rewrite.md, 20-analysis-work-level.yaml) | Grep 4 | Bash 3 (on-disk yaml key verification, byte counts, dispatch grep)
- Tavily-first: N/A — no external/web lookups required (all verification was local-file-bound: design pack, P3 run evidence, on-disk artifacts).

## Issues Found
| # | Severity | Location | Issue | Required Fix |
|---|----------|----------|-------|-------------|
| 1 | CRITICAL | `laf-adaptation/agents/analyst.md` (frontmatter `skills:` + body lines 47, 74); `laf-adaptation/agents/tier-coordinator.md` (frontmatter `skills:` + body lines 95-99, 178-181) | **`thematic-fidelity` is referenced in both agent bodies but loaded by NEITHER.** `analyst.md`'s body says the `meaning:` field is "Grounded in `/thematic-fidelity`"; `tier-coordinator.md`'s Check D is headed "(R10; /thematic-fidelity)" and the body asserts "no new skill line is required" — but neither agent's `skills:` frontmatter lists `laf-adaptation:thematic-fidelity`. Per the boundary contract (`laf-adaptation/CLAUDE.md` §2), the ONLY sanctioned path for NATIVE knowledge to enter an agent is the `skills:` frontmatter line; an unloaded `/thematic-fidelity` reference is unreachable at runtime. The P3 run could only produce `meaning:` because the prep-cordinator (which DOES list `thematic-fidelity`) drove the procedure; a per-chapter dispatch of `analyst` or `tier-coordinator` outside the prep-cordinator's umbrella would have NO access to the meaning-preservation concept the bodies claim to enforce. The "no new skill line is required" claim in `tier-coordinator.md:181` is self-contradicting: it asserts Check D needs no skill line, then relies on `/thematic-fidelity` to define `preserves_meaning()`. | Add `- laf-adaptation:thematic-fidelity` to the `skills:` frontmatter of BOTH `analyst.md` and `tier-coordinator.md`. Either keep the "no new skill line" sentence (now-true after the add) OR delete it as superseded. Re-run `uv run python laf-adaptation/scripts/check_boundary.py` (Mode V) — Rule F will require the two agents' SKILL globs to remain covered; the new skill line is additive and boundary-safe (NATIVE skill, no adopted-body drift). |
| 2 | IMPORTANT | `laf-adaptation/skills/prep/SKILL.md` §6 (lines 93-107); `laf-adaptation/agents/prep-cordinator.md` Stage 7 (lines 57-60 headline vs. 87-94 operator-clarity); `laf-adaptation/skills/prep/resources/path-contract.md` §5 (write-ownership table lines 73-74 vs. form-transform-operator note lines 80-85) | **Self-contradictory attribution of the dual-form transform operator.** Each of the three built files contains the same pattern: a HEADLINE statement ("promote `30-mapping.yaml` (dual-form) via `/kb-management`" / write-ownership rows "Written by: `/kb-management`") immediately contradicted by an "operator clarity" note stating the **coordinator** owns the transform and `/kb-management` "does NOT itself perform the 6-key↔5-key transform … its body has no awareness of the dual form." I verified the latter is true: `kb-management/SKILL.md` has zero mention of `meaning:` stripping, `concept_mapping/templates/`, hyphen-vs-underscore naming, or the prep promotion step. A reader who stops at the headline will believe `/kb-management` performs the dual-form transform — which is false. The contradiction is preserved verbatim across three files, so it is systemic, not a one-off typo. | In each of the three files, reconcile the headline with the operator-clarity note. Recommended phrasing for the headline: "promote `30-mapping.yaml` (dual-form): the **prep-cordinator** derives both target files (6-key hyphen keeping `meaning:`, 5-key underscore stripping `meaning:` and `_confidence`); `/kb-management` is invoked for the kb-lifecycle write (registry acceptance) only." Update the `path-contract.md` §5 write-ownership table rows for the two promotion targets from "Written by: `/kb-management`" to "Written by: `prep-cordinator` (dual-form transform) + `/kb-management` (kb-lifecycle write)". |
| 3 | IMPORTANT | `laf-adaptation/agents/prep-cordinator.md:13` (`tools:` frontmatter line) | **`tier-coordinator` declared as a dispatchable subagent in `tools:` but never dispatched by any of the 8 prep stages.** The tools line reads `Agent(web-researcher, analyst, tier-coordinator)`. The 8-stage body (lines 46-61) dispatches only `web-researcher` (Stage 1c) and `analyst` (Stage 3). `tier-coordinator` is the rewrite-phase step-10 agent (`tier-coordinator.md:21` "workflow step 10"), and the prep phase explicitly ends at Stage 8 handoff (`/laf:rewrite --work <slug>`). The rewrite phase — a separate session — is where `tier-coordinator` runs. Listing it in the prep-cordinator's `tools:` overclaims the agent's dispatch surface and misleads readers about which agents the prep phase orchestrates. | Remove `tier-coordinator` from `prep-cordinator.md:13` `tools:` line, leaving `Agent(web-researcher, analyst)`. The design pack (`prep-agent-schemas.md` §1) and DESIGN.md §3 data-flow corroborate that only `web-researcher` and `analyst` are dispatched during prep. |
| 4 | IMPORTANT | `laf-adaptation/agents/tier-coordinator.md:97` (Check D body) | **Ambiguous/wrong filename token in Check D's `meaning:` source.** The body reads "Read the top-level `meaning:` from `<work>-mapping.yaml` (and the per-unit `meaning` in `shared_analysis`)." But per `path-contract.md` §3 there are TWO promoted files with distinct naming conventions: the 0.1 hyphen copy `<slug>-mapping.yaml` (e.g. `tolkien-mapping.yaml`) which KEEPS `meaning:`, and the root underscore copy `<slug>_mapping.yaml` (e.g. `tolkien_mapping.yaml`) which STRIPS `meaning:`. The token `<work>-mapping.yaml` is ambiguous (bare `<work>` could be either) and, read literally against the underscore convention, points at the meaning-stripped root copy — the one file that CANNOT satisfy the read. Check D would fail to find `meaning:` if the reader resolves the reference to the underscore file. | Replace "`<work>-mapping.yaml`" with the explicit hyphen form: "Read the top-level `meaning:` from `kb/adaptation-mapping/<slug>-mapping.yaml` (the 0.1 hyphen copy that keeps `meaning:`; the root underscore copy strips it — see `path-contract.md` §3)." This also aligns with Check D's own invariant that `meaning:` must be present to evaluate `preserves_meaning()`. |

## Cross-references to other Phase-6 QA reports (no contradiction with this lens)

- `qa-p3-fidelity-verification-content-report.md` and `qa-p3-fidelity-verification-structural-report.md` independently confirm the P3 dual-form SHAPE contract (6-key/5-key split, write-provenance) — consistent with this report's checks 2-5.
- `qa-p1-meaning-flow-report.md` traces the `meaning:` flow analyst→mapping→muse — consistent with this report's checks 7-8, 16. The meaning-flow finding there is structural (does the field propagate); THIS report's CRITICAL finding #1 is the orthogonal runtime-loadability gap (can `analyst`/`tier-coordinator` actually load `/thematic-fidelity` when run outside the prep-cordinator umbrella). Both are real; they do not duplicate.

## Notes on the design-vs-run Narnia/Tolkien substitution (check 13)

DESIGN.md §7 P3 row still specifies Narnia/narnia; the run used Tolkien/tolkien. This is fully authorized by `research/07-gap-fill.md` GAP 3 (the repo ships no Narnia source, only the synthetic Tolkien fixture `source/tolkien/ch-01.txt`). The built outputs under review here (`prep/SKILL.md`, `prep-cordinator.md`, `commands/laf/prep.md`, `commands/laf/rewrite.md`) all use `<slug>` placeholders, so they do NOT carry the stale Narnia spec forward — hence PASS for the built-output lens. The DESIGN.md §7 row itself is a stale-spec item outside this lens's scope and is flagged here for completeness only; it is not counted in the Issues Found table because DESIGN.md is the spec, not a built runtime artifact.

## Self-Audit

**(a) Reliance list — items where P3 evidence was trusted without independent re-execution:**
- Relied on `p3-dualform.txt` assertions (a)/(b)/(c)/(d) for the dual-form shape contract — partially re-verified independently (see (b)).
- Relied on `p3-package-assert.md` (c) for the `min(text,context)` per-entry claim — the pyyaml programmatic verification was not re-run.
- Relied on `p3-rewrite-assert.md` A1-A4 for the rewrite command resolution + greenlight guard + muse hand-off — not independently re-executed.

**(b) Independent semantic checks (≥1 required, INV-019):**
- On-disk yaml key verification via `uv run --with pyyaml python -c …` against `work/prep/tolkien/30-mapping.yaml`, `laf-adaptation/kb/adaptation-mapping/tolkien-mapping.yaml`, `config/concept_mapping/templates/tolkien_mapping.yaml` — confirmed 6/6/5 keys with/without `meaning:` independently of `p3-dualform.txt` (Bash tool, three files).
- Read `kb-management/SKILL.md` in full (124 lines) to verify the CRITICAL finding that `/kb-management` has zero dual-form awareness — the body contains no `meaning:`, no `concept_mapping/templates/`, no hyphen/underscore logic. This was the load-bearing evidence for Issue #2 and is independent of any P3 assertion.
- Grep-verified `thematic-fidelity` is absent from both `analyst.md` and `tier-coordinator.md` `skills:` frontmatter (Grep tool, two files) — load-bearing for Issue #1.
- Grep-verified `tier-coordinator` is referenced in `prep-cordinator.md` ONLY on the `tools:` line (line 13) and nowhere in the 8-stage body — load-bearing for Issue #3.

## Recommendations

Before declaring Phase 6 done, resolve ALL four findings (no severity exempt per the QA verdict rule):

1. **CRITICAL fix (Issue #1).** Add `- laf-adaptation:thematic-fidelity` to the `skills:` frontmatter of `analyst.md` and `tier-coordinator.md`. Re-run `uv run python laf-adaptation/scripts/check_boundary.py` (Mode V) — expect PASS (additive NATIVE skill line, no adopted-body drift). This is the only finding that affects runtime correctness of the meaning-preservation feature outside the prep-cordinator's umbrella.
2. **IMPORTANT fix (Issue #2).** Reconcile the three dual-form-attribution contradictions in `prep/SKILL.md` §6, `prep-cordinator.md` Stage 7, and `path-contract.md` §5. Make the headline attribute the transform to the coordinator; keep `/kb-management` scoped to the kb-lifecycle write.
3. **IMPORTANT fix (Issue #3).** Remove `tier-coordinator` from `prep-cordinator.md:13` `tools:` line.
4. **IMPORTANT fix (Issue #4).** Disambiguate the `<work>-mapping.yaml` reference in `tier-coordinator.md:97` Check D to the explicit hyphen form.
5. **Out-of-scope note.** DESIGN.md §7 P3 row's stale Narnia/narnia spec should be corrected to Tolkien/tolkien in a follow-up spec-refresh pass (authorized substitution per `research/07-gap-fill.md` GAP 3). Not blocking — built runtime artifacts already use `<slug>` placeholders.

## QA Complete
